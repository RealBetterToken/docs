---
title: "OpenClaw Setup: API Key, Base URL, and Custom Models"
sidebarTitle: "OpenClaw"
description: "Configure OpenClaw models.providers, a BetterToken API Key, Base URL, OpenAI Responses or Chat Completions models, and validate the Gateway."
icon: "paw"
---

OpenClaw connects to BetterToken through `~/.openclaw/openclaw.json`. GPT provider models use `openai-responses`; other supported providers use `openai-completions`.

## Key settings

| Field | Value |
| --- | --- |
| API Key | BetterToken API Key |
| Base URL | `{{openai-base-url}}` |
| Model | `YOUR_MODEL_ID` |

## Prerequisites

- Install the latest OpenClaw
- <a href={"{{register-url}}"}>Create a BetterToken API Key</a>
- Copy a Model ID from the <a href={"{{model-plaza-url}}"}>model plaza</a> or key **Setup** dialog

## Install

<Tabs>
  <Tab title="macOS / Linux">
    ```bash
    curl -fsSL https://openclaw.ai/install.sh | bash
    ```
  </Tab>
  <Tab title="Windows">
    ```powershell
    iwr -useb https://openclaw.ai/install.ps1 | iex
    ```
  </Tab>
</Tabs>

## Command-line setup

The BetterToken Automatic setup script writes the OpenClaw configuration. It requires Node.js. If you do not pass an API Key or Model ID as an argument, the script prompts you for them.

<Tabs>
  <Tab title="macOS / Linux">
    ```bash
    curl -fsSL "{{install-openclaw-provider-sh-url}}" | bash
    ```
  </Tab>
  <Tab title="Windows">
    ```powershell
    iwr "{{install-openclaw-provider-ps1-url}}" -OutFile "$env:TEMP\install-openclaw-provider.ps1"; powershell -ExecutionPolicy Bypass -File "$env:TEMP\install-openclaw-provider.ps1"
    ```
  </Tab>
</Tabs>

Confirm that `agents.defaults.model.primary` uses the `YOUR_MODEL_ID` shown in Setup, then run the validation and Gateway restart commands below.

## Manual setup

### Configure OpenClaw

Edit `~/.openclaw/openclaw.json`. Use only the example that matches the provider of your Model ID.

<Tabs>
  <Tab title="GPT: openai-responses">
    ```json
    {
      "models": {
        "mode": "merge",
        "providers": {
          "bettertoken": {
            "baseUrl": "{{openai-base-url}}",
            "apiKey": "YOUR_API_KEY",
            "api": "openai-responses",
            "models": [
              {
                "id": "YOUR_MODEL_ID",
                "name": "YOUR_MODEL_ID"
              }
            ]
          }
        }
      },
      "agents": {
        "defaults": {
          "model": {
            "primary": "bettertoken/YOUR_MODEL_ID"
          }
        }
      }
    }
    ```
  </Tab>
  <Tab title="Non-GPT: openai-completions">
    ```json
    {
      "models": {
        "mode": "merge",
        "providers": {
          "bettertoken": {
            "baseUrl": "{{openai-base-url}}",
            "apiKey": "YOUR_API_KEY",
            "api": "openai-completions",
            "models": [
              {
                "id": "YOUR_MODEL_ID",
                "name": "YOUR_MODEL_ID"
              }
            ]
          }
        }
      },
      "agents": {
        "defaults": {
          "model": {
            "primary": "bettertoken/YOUR_MODEL_ID"
          }
        }
      }
    }
    ```
  </Tab>
</Tabs>

Do not infer `api` from a model name. GPT uses `openai-responses`. Use `openai-completions` for a non-GPT provider only when the model plaza or Setup dialog confirms compatibility.

## Verify the connection

Run:

```bash
openclaw config validate
openclaw gateway restart
openclaw models list
openclaw models status
```

The setup is active when validation passes, the Gateway restarts, and `bettertoken/YOUR_MODEL_ID` appears in the model list and status. If an existing session still uses an old model, start a new session and check again.

## Switch models

Add the new model to `models.providers.bettertoken.models`, then change `agents.defaults.model.primary` to `bettertoken/YOUR_MODEL_ID`. Save, validate, and restart the Gateway.

## Common errors

| Error | Fix |
| --- | --- |
| `config validate` fails | Check JSON commas, quotes, and brackets. |
| `401` | Copy `apiKey` again and remove spaces. |
| `404` or protocol error | GPT must use `openai-responses`; non-GPT must use `openai-completions`. Do not append an endpoint to the Base URL. |
| Model missing | Confirm it exists in both the provider model list and `agents.defaults.model.primary`. |
| Existing session uses the old model | Run `openclaw gateway restart` and create a new session. |

## Advanced setup

{/* tool-provider-support:start */}
### Supported providers

| Provider | Status |
| --- | --- |
| Claude | Not supported |
| GPT | Command-line + manual setup |
| Kimi | Manual setup |
| GLM | Manual setup |

<Note>Statuses apply to the BetterToken setup documented on this page.</Note>

<Accordion title="What the setup methods mean">
  - **Command-line + manual setup**: use a generated command or follow the complete manual steps.
  - **Manual setup**: enter the API Key, Base URL, and Model.
  - **Not supported**: no verified direct setup is currently available.
</Accordion>
{/* tool-provider-support:end */}

### Related FAQ

- [OpenAI-compatible API vs Anthropic-compatible API](/en/faq/concepts/openai-compatible-vs-anthropic-compatible)
- [MCP vs API Key and Base URL](/en/faq/concepts/mcp-vs-api-key-base-url)
- [What are model_provider, base_url, and wire_api?](/en/faq/codex/model-provider-base-url-wire-api)
- [How to configure an OpenAI-compatible API in Cline](/en/faq/cline/openai-compatible-api)

### Optional: manage the provider with CC Switch

To manage providers for several tools in one place, see [OpenClaw setup in CC Switch](/en/ai-tools/cc-switch#openclaw).

## Technical details

<Accordion title="Responses and Chat Completions">
  `openai-responses` makes OpenClaw call `/v1/responses` for GPT provider models. `openai-completions` calls `/v1/chat/completions` for confirmed compatible non-GPT providers. Both use `{{openai-base-url}}` as `baseUrl`.
</Accordion>
