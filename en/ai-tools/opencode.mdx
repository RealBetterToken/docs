---
title: "OpenCode Custom Models: API Key, Base URL, and JSON Setup"
sidebarTitle: "OpenCode"
description: "Connect OpenCode to BetterToken OpenAI-compatible Chat Completions with /connect, opencode.json, an API Key, Base URL, and Model ID."
icon: "brackets-curly"
---

OpenCode connects to BetterToken with `@ai-sdk/openai-compatible`. Name the OpenCode provider **BetterToken** or **OpenAI Compatible**. GPT, Kimi, and GLM are model providers inside BetterToken, not OpenCode provider names. For Kimi K3, follow the dedicated [Kimi K3 API in OpenCode guide](/en/faq/opencode/kimi-k3-api).

## Key settings

| Field | Value |
| --- | --- |
| API Key | BetterToken API Key |
| Base URL | `{{openai-base-url}}` |
| Model | `YOUR_MODEL_ID` |

## Prerequisites

- Install the latest OpenCode
- <a href={"{{register-url}}"}>Create a BetterToken API Key</a>
- Copy a Model ID from the <a href={"{{model-plaza-url}}"}>model plaza</a> or key **Setup** dialog

## Install

<Tabs>
  <Tab title="curl">
    ```bash
    curl -fsSL https://opencode.ai/install | bash
    ```
  </Tab>
  <Tab title="npm">
    ```bash
    npm install -g opencode-ai
    ```
  </Tab>
</Tabs>

## Command-line setup

The BetterToken Automatic setup script creates or updates the BetterToken provider in OpenCode. It requires Node.js. If you do not pass an API Key or Model ID as an argument, the script prompts you for them.

<Tabs>
  <Tab title="macOS / Linux">
    ```bash
    curl -fsSL "{{install-opencode-provider-sh-url}}" | bash
    ```
  </Tab>
  <Tab title="Windows">
    ```powershell
    iwr "{{install-opencode-provider-ps1-url}}" -OutFile "$env:TEMP\install-opencode-provider.ps1"; powershell -ExecutionPolicy Bypass -File "$env:TEMP\install-opencode-provider.ps1"
    ```
  </Tab>
</Tabs>

Select `bettertoken/YOUR_MODEL_ID` after the script finishes. If OpenCode is already running, restart it before testing.

## Manual setup

### Option 1: save credentials with /connect

Start OpenCode and run:

```text
/connect
```

Choose **Other**, enter `bettertoken` as the provider id, and then enter `YOUR_API_KEY`. OpenCode stores the credential locally.

### Option 2: configure JSON

The global file is `~/.config/opencode/opencode.json`. A project can use `opencode.json` in its root.

```json
{
  "$schema": "https://opencode.ai/config.json",
  "model": "bettertoken/YOUR_MODEL_ID",
  "provider": {
    "bettertoken": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "BetterToken",
      "options": {
        "baseURL": "{{openai-base-url}}"
      },
      "models": {
        "YOUR_MODEL_ID": {
          "name": "YOUR_MODEL_ID"
        }
      }
    }
  }
}
```

If you do not use `/connect`, add `"apiKey": "YOUR_API_KEY"` under `options`. Never commit a configuration containing a real key.

## Verify the connection

Run `opencode` in a project, select `bettertoken/YOUR_MODEL_ID`, and send a short message. A normal response confirms the setup. Restart OpenCode after changing JSON.

## Switch models

Add a new `YOUR_MODEL_ID` under `provider.bettertoken.models`, update the top-level `model`, save, and restart.

## Common errors

| Error | Fix |
| --- | --- |
| `401` | Run `/connect` again or check `apiKey`. |
| `404` | Use only `{{openai-base-url}}` as `baseURL`; do not append `/chat/completions`. |
| Model not found | Make the top-level `model` exactly match an ID under `models`. |
| Configuration not applied | Check whether project `opencode.json` overrides the global file, then restart OpenCode. |

## Advanced setup

{/* tool-provider-support:start */}
### Supported providers

| Provider | Status |
| --- | --- |
| Claude | Not supported |
| GPT | Command-line + manual setup |
| Kimi | Manual setup |
| GLM | Manual setup |

<Note>Statuses apply to the BetterToken setup documented on this page.</Note>

<Accordion title="What the setup methods mean">
  - **Command-line + manual setup**: use a generated command or follow the complete manual steps.
  - **Manual setup**: enter the API Key, Base URL, and Model.
  - **Not supported**: no verified direct setup is currently available.
</Accordion>
{/* tool-provider-support:end */}

### Related FAQ

- [How to connect Kimi K3 to OpenCode](/en/faq/opencode/kimi-k3-api)
- [How to connect GLM to OpenCode](/en/faq/opencode/glm-api)
- [OpenAI-compatible API vs Anthropic-compatible API](/en/faq/concepts/openai-compatible-vs-anthropic-compatible)
- [MCP vs API Key and Base URL](/en/faq/concepts/mcp-vs-api-key-base-url)
- [What are model_provider, base_url, and wire_api?](/en/faq/codex/model-provider-base-url-wire-api)
- [How to configure an OpenAI-compatible API in Cline](/en/faq/cline/openai-compatible-api)

### Optional: manage the provider with CC Switch

To manage providers for several tools in one place, see [OpenCode setup in CC Switch](/en/ai-tools/cc-switch#opencode).

## Technical details

<Accordion title="Provider and model provider">
  The OpenCode provider is `bettertoken`, backed by `@ai-sdk/openai-compatible` and Chat Completions. GPT, Kimi, and GLM are model providers in BetterToken; put the selected complete Model ID in `models`.
</Accordion>
