---
title: "Cursor Custom API: OpenAI Base URL and API Key Setup"
sidebarTitle: "Cursor"
description: "Configure BetterToken as a Cursor custom API with an OpenAI Base URL and API Key, select a GPT Key group model, and fix common setup errors."
icon: "arrow-pointer"
---

To connect BetterToken as a Cursor custom API, turn on **Override OpenAI Base URL**, enter `{{openai-base-url}}`, paste your API Key into **OpenAI API Key**, and select a model from the **GPT Key group**. Copy current model IDs from the <a href={"{{model-plaza-url}}"}>model plaza</a>.

## Cursor quick values

| Cursor field | Value |
| --- | --- |
| **Override OpenAI Base URL** | Turn on |
| **Base URL** | `{{openai-base-url}}` |
| **OpenAI API Key** | Your BetterToken API Key from the **GPT Key group** |
| Custom model | A current model ID from the same **GPT Key group** |

<Warning>
  Cursor only lets users on a paid advanced tier or higher configure custom models. If your account or client version does not show custom model, API Key, or Base URL options, check your Cursor plan and client version first.
</Warning>

<Warning>
  **Known issue:** **Override OpenAI Base URL** is a global setting. When enabled, it affects all API keys configured in Cursor, including Anthropic and GPT keys used by Cursor's built-in models. Cursor's official community has confirmed this behavior: setting a Base URL affects all API keys and models ([community thread](https://forum.cursor.com/t/cursor-models-fail-when-using-byok-openai-key-with-overridden-base-url-glm-4-7/147218)).

  If Cursor's built-in Claude / GPT models stop working after you enable **Override OpenAI Base URL**, turn **Override OpenAI Base URL** off when you are not using BetterToken. Cursor currently does not support a different Base URL per model; that capability is still tracked as a feature request ([feature request](https://forum.cursor.com/t/custom-base-urls-for-each-custom-model/147219)).
</Warning>

## Prerequisites

- Cursor installed ([download Cursor](https://www.cursor.com))
- BetterToken API Key (<a href={"{{register-url}}"}>register here</a>)
- The API Key belongs to the **GPT Key group**, and you plan to select a model from the same group

## Setup

<Steps>
  <Step title="Open the model settings page">
    In Cursor, click **Settings** in the lower-left corner, then go to **Models**. Scroll down to **API Keys**.

    <Frame>
      <img
        src="/images/cursor/settings-api-key.png"
        alt="Cursor settings page showing Settings, Models, API Keys, OpenAI API Key, and Override OpenAI Base URL fields."
        style={{ borderRadius: '0.5rem' }}
      />
    </Frame>
  </Step>

  <Step title="Enter the Base URL and API Key">
    In **API Keys**, configure the fields in this order:

    1. Enable **Override OpenAI Base URL**
    2. Enter `{{openai-base-url}}` in the Base URL field
    3. Paste your BetterToken API Key into **OpenAI API Key**
    4. After the URL and key are filled in, enable the **OpenAI API Key** toggle

    Enter the key before turning on the toggle. This makes Cursor show the authentication confirmation dialog.
  </Step>

  <Step title="Enable the OpenAI API Key">
    In the confirmation dialog, click **Enable OpenAI API Key**.

    <Frame>
      <img
        src="/images/cursor/enable-openai-api-key.png"
        alt="Cursor confirmation dialog asking you to enable your own OpenAI API key."
        style={{ borderRadius: '0.5rem' }}
      />
    </Frame>
  </Step>

  <Step title="Refresh the model list and enable models">
    Back in **Models**, click the refresh button on the right before selecting a model. Wait until the model list finishes refreshing.

    Only select models that belong to the same BetterToken group as your API Key. When using a GPT Key group API Key, choose a model ID from the **GPT Key group** in the model plaza, then turn on the toggle for that model.

    <Frame>
      <img
        src="/images/cursor/models-refresh-select.png"
        alt="Cursor Models section showing the refresh button and model toggles."
        style={{ borderRadius: '0.5rem' }}
      />
    </Frame>
  </Step>

  <Step title="Return to chat and turn off Auto">
    After setup, return to the Cursor chat screen and open the model picker below the input box. If **Auto** is enabled, turn **Auto** off first.

    <Frame>
      <img
        src="/images/cursor/chat-disable-auto.png"
        alt="Cursor chat model picker showing the Auto toggle that should be turned off."
        style={{ borderRadius: '0.5rem' }}
      />
    </Frame>
  </Step>

  <Step title="Select the model and start chatting">
    Choose the GPT Key group model you enabled, then start chatting.

    <Frame>
      <img
        src="/images/cursor/chat-select-model.png"
        alt="Cursor chat input with an enabled model selected from the model picker."
        style={{ borderRadius: '0.5rem' }}
      />
    </Frame>
  </Step>
</Steps>

## Custom API and API Key errors

### Cursor does not show Custom API, API Key, or Base URL fields

Check your Cursor plan and client version. Custom models are available only in supported paid tiers. After updating Cursor, open **Settings** → **Models** again.

### The API Key cannot be enabled or verified

Enter the Base URL `{{openai-base-url}}` and the API Key before turning on the **OpenAI API Key** toggle. Do not append `/chat/completions` to the Base URL, and do not use a Claude Key group key.

### The model is missing after refresh

Confirm that the API Key belongs to the **GPT Key group**, wait for refresh to finish, and enable only a model from that same group. The model plaza always lists current IDs.

### Cursor built-in models stop working

**Override OpenAI Base URL** is global. When you are not using BetterToken, turn this toggle off so Cursor can use its own Base URL again.

### Chat selects a different model

Open the model picker and turn **Auto** off. Then manually select the model you enabled.

## Related FAQ

- [How to configure a custom OpenAI API Key and Base URL in Cursor](/en/faq/cursor/custom-openai-api)
- [Cursor Rules, AGENTS.md, and .cursorignore explained](/en/faq/cursor/rules-agents-cursorignore)
- [How to configure MCP in Cursor](/en/faq/cursor/mcp)
- [Claude Code vs Cursor for local development](/en/faq/claude-code/claude-code-vs-cursor)
- [Configure a custom provider in Codex CLI](/en/ai-tools/codex)
- [Configure an OpenAI-compatible API in Cline](/en/ai-tools/cline)
- [OpenAI-compatible API vs Anthropic-compatible API](/en/faq/concepts/openai-compatible-vs-anthropic-compatible)

## Notes

- Do not use Claude Key group model IDs in the Codex / OpenAI-compatible flow
- Do not keep using hardcoded Claude model names; use a current GPT Key group model ID from the <a href={"{{model-plaza-url}}"}>model plaza</a>
