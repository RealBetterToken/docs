---
title: "Extensão Codex para VS Code: Base URL personalizada, API Key e login oficial"
sidebarTitle: "Codex VS Code Extension"
description: "Configure o config.toml compartilhado da extensão Codex para VS Code, use Responses da BetterToken e preserve o login oficial, os plugins e o Remote Control."
icon: "/icons/codex-vscode.svg"
---

A extensão Codex para VS Code compartilha `~/.codex/config.toml` com o Codex CLI/App. Use esta página quando precisar manter o login oficial, os plugins ou o Remote Control.

## Configurações principais

| Campo | Valor |
| --- | --- |
| API Key | `experimental_bearer_token = "YOUR_API_KEY"` |
| Base URL | `{{openai-base-url}}` |
| Modelo | `gpt-6-astra` |

## Pré-requisitos

- Instale a versão mais recente da extensão Codex para VS Code
- Conclua o login oficial na extensão ou no Codex App
- <a href={"{{register-url}}"}>Crie uma BetterToken API Key</a>
- Copie um ID de modelo da <a href={"{{model-plaza-url}}"}>model plaza</a> ou da caixa **Setup** da chave

## Instalação

Instale a extensão pelo VS Code Marketplace e reinicie o VS Code.

## Configuração manual

Se você definiu um `CODEX_HOME` personalizado, edite `config.toml` nesse diretório. Caso contrário, use `~/.codex/config.toml` no macOS / Linux ou `%USERPROFILE%\.codex\config.toml` no Windows.

Feche o Codex completamente e faça backup dos arquivos `config.toml` e `auth.json` existentes. Atualize as configurações do provedor existente sem adicionar uma tabela de provedor duplicada.

### Configure a extensão

Edite `~/.codex/config.toml`:

```toml
model_provider = "bettertoken"
model = "gpt-6-astra"

[model_providers.bettertoken]
name = "BetterToken"
base_url = "{{openai-base-url}}"
wire_api = "responses"
requires_openai_auth = true
experimental_bearer_token = "YOUR_API_KEY"
```

Salve o arquivo, execute **Developer: Reload Window** no VS Code e abra novamente o painel do Codex.

## Teste a conexão

Crie uma nova sessão na extensão e envie uma tarefa curta. Uma resposta de `gpt-6-astra`, com os plugins e o Remote Control ainda disponíveis, confirma a configuração.

## Alterne entre modelos

Altere `model` e, opcionalmente, `review_model`. Depois, execute **Developer: Reload Window**. Use um ID da caixa Setup ou da model plaza.

## Erros comuns

| Erro | Correção |
| --- | --- |
| A extensão pede login novamente | Restaure o login oficial e mantenha `requires_openai_auth = true`. |
| `401` ou `403` | Copie `experimental_bearer_token` novamente; não sobrescreva `auth.json`. |
| Tabela duplicada | Remova a outra tabela `[model_providers.bettertoken]` e mantenha um único modo de autenticação. |
| Modelo não encontrado | Substitua `gpt-6-astra` por um ID completo e atual. |
| Configuração não aplicada | Salve o arquivo e execute **Developer: Reload Window**. |

## Configuração avançada

{/* tool-provider-support:start */}
### Provedores compatíveis

| Provedor | Status |
| --- | --- |
| Claude | Sem suporte |
| GPT | Configuração manual |
| Kimi | Configuração manual |
| GLM | Configuração manual |

<Note>Os status se aplicam ao método de configuração do BetterToken descrito nesta página.</Note>

<Accordion title="O que significa cada método de configuração">
  - **Configuração manual**: informe a API Key, a Base URL e o Model.
  - **Sem suporte**: ainda não há um método verificado de conexão direta.
</Accordion>
{/* tool-provider-support:end */}

### Guias relacionados

- [Provedor personalizado no Codex CLI/App](/pt-br/ai-tools/codex)
- [Preservar o login oficial do Codex com uma API de terceiros](/pt-br/faq/codex/official-login-third-party-api)
- [Guia de configuração do config.toml do Codex](/pt-br/faq/codex/config-toml)
- [O que são model_provider, base_url e wire_api?](/pt-br/faq/codex/model-provider-base-url-wire-api)

### Referências oficiais

- [Extensão do Codex para IDE](https://developers.openai.com/codex/ide)
- [Configuração básica do Codex](https://developers.openai.com/codex/config-basic)
- [Referência de configuração do Codex](https://developers.openai.com/codex/config-reference)

## Detalhes técnicos

<Accordion title="Protocolo Responses">
  A extensão envia as solicitações do modelo para `{{openai-base-url}}` por meio do OpenAI Responses.
</Accordion>
