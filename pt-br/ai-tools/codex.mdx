---
title: "Provedor personalizado do Codex CLI/App: config.toml, API Key e Base URL"
sidebarTitle: "Codex"
description: "Configure Codex CLI e Codex App com config.toml, auth.json, uma BetterToken API Key e o Base URL de Responses."
icon: "code"
---

Codex CLI e Codex App compartilham `config.toml` e `auth.json` para tarefas locais. Esta configuração usa uma BetterToken API Key via OpenAI Responses.

## Configurações principais

| Campo | Valor |
| --- | --- |
| API Key | `auth.json` → `OPENAI_API_KEY` |
| Base URL | `{{openai-base-url}}` |
| Model | `gpt-6-astra` |

## Pré-requisitos

- Instale o Node.js
- <a href={"{{register-url}}"}>Crie uma BetterToken API Key</a>
- Copie um Model ID do <a href={"{{model-plaza-url}}"}>model plaza</a> ou da caixa de diálogo **Setup** da Key

## Instalação

```bash
npm install -g @openai/codex
```

## Localize os arquivos e faça backup

Se você definiu um `CODEX_HOME` personalizado, edite `config.toml` e `auth.json` nesse diretório. Caso contrário, use `~/.codex` no macOS / Linux ou `%USERPROFILE%\.codex` no Windows. CLI e App devem usar o mesmo diretório. O WSL tem seu próprio diretório de usuário.

Feche o Codex completamente antes de editar. Faça backup dos dois arquivos em um local privado. Crie os arquivos que faltarem. Substitua as configurações existentes sem duplicar chaves ou tabelas; preserve as demais configurações.

<Warning>
  Esta configuração troca o login com ChatGPT pela autenticação com API Key. Ela não preserva o acesso à conta oficial. Proteja `auth.json` e seus backups; não os compartilhe nem os inclua no repositório.
</Warning>

## Configuração pela linha de comando

O script de configuração automática do BetterToken grava a configuração do provedor para o Codex. Ele exige Node.js e solicita a API Key ou o Model ID quando esses valores não são informados como argumentos.

<Tabs>
  <Tab title="macOS / Linux">
    ```bash
    curl -fsSL "{{install-codex-provider-sh-url}}" | bash
    ```
  </Tab>
  <Tab title="Windows">
    ```powershell
    iwr "{{install-codex-provider-ps1-url}}" -OutFile "$env:TEMP\install-codex-provider.ps1"; powershell -ExecutionPolicy Bypass -File "$env:TEMP\install-codex-provider.ps1"
    ```
  </Tab>
</Tabs>

Após a instalação, compare os dois arquivos com a configuração manual abaixo e corrija as diferenças antes de iniciar o Codex.

## Configuração manual

### Edite config.toml

Coloque `model_provider`, `model` e `cli_auth_credentials_store` no nível superior, antes de todas as tabelas TOML, não dentro de um perfil. O ID do provedor deve corresponder ao nome da tabela. Para esta autenticação por arquivos, remova `env_key` e `experimental_bearer_token` do provedor selecionado.

```toml
model_provider = "bettertoken"
model = "gpt-6-astra"
cli_auth_credentials_store = "file"

[model_providers.bettertoken]
name = "BetterToken"
base_url = "{{openai-base-url}}"
wire_api = "responses"
requires_openai_auth = true
request_max_retries = 4
stream_max_retries = 8
stream_idle_timeout_ms = 300000
supports_websockets = false
```

O exemplo usa GPT-6 Astra (`gpt-6-astra`). Para outro modelo, copie o ID completo da <a href={"{{model-plaza-url}}"}>model plaza</a> e substitua `model`. GPT, Kimi e GLM usam a mesma estrutura de autenticação por arquivos; a configuração automática está disponível para GPT.

### Edite auth.json

`auth.json` fica no mesmo diretório que `config.toml`. Se `auth.json` não existir, crie esse arquivo no mesmo diretório. Use o JSON abaixo como conteúdo do arquivo, substitua `YOUR_API_KEY` pela sua BetterToken API Key e salve o arquivo.

```json
{
  "auth_mode": "apikey",
  "OPENAI_API_KEY": "YOUR_API_KEY"
}
```

## Verificar a conexão

Salve os dois arquivos. Inicie uma nova sessão do Codex CLI diretamente pelo terminal:

```bash
codex
```

Feche o Codex App completamente, incluindo os processos em segundo plano. Abra-o novamente e crie uma nova tarefa local. Envie um prompt curto em cada cliente que usa. Uma resposta normal confirma a conexão desse cliente.

## Trocar de modelo

Altere `model` e, opcionalmente, `review_model`. Salve o arquivo, encerre o processo atual do Codex e inicie-o novamente. Use o Model ID da caixa de diálogo Setup ou do model plaza.

## Erros comuns

Em caso de `401`, `403` ou solicitação de login, confira o diretório ativo, a Key em `auth.json`, `auth_mode = apikey`, `cli_auth_credentials_store = file` e `requires_openai_auth = true`. Para erros de provedor ou modelo, confira o ID do provedor, `{{openai-base-url}}`, `wire_api = responses` e a disponibilidade do modelo. Se as alterações não forem aplicadas, verifique as opções que sobrescrevem a configuração e confirme que CLI e App usam o mesmo `CODEX_HOME`.

## Configuração avançada

{/* tool-provider-support:start */}
### Provedores compatíveis

| Provedor | Status |
| --- | --- |
| Claude | Sem suporte |
| GPT | Linha de comando e configuração manual |
| Kimi | Configuração manual |
| GLM | Configuração manual |

<Note>Os status se aplicam ao método de configuração do BetterToken descrito nesta página.</Note>

<Accordion title="O que significa cada método de configuração">
  - **Configuração pela linha de comando e manual**: use um comando gerado ou siga todas as etapas manuais.
  - **Configuração manual**: informe a API Key, a Base URL e o Model.
  - **Sem suporte**: ainda não há um método verificado de conexão direta.
</Accordion>
{/* tool-provider-support:end */}

### Extensão Codex

A extensão Codex para VS Code compartilha `config.toml`, mas manter o login oficial exige uma configuração de autenticação diferente. Consulte o [guia de configuração da extensão Codex](/pt-br/ai-tools/codex-vscode).

### Perguntas relacionadas

- [O que é Codex CLI](/pt-br/faq/codex/what-is-codex-cli)
- [Codex CLI, Codex App ou Codex Web](/pt-br/faq/codex/codex-cli-vs-app-vs-web)
- [Guia de configuração de config.toml no Codex](/pt-br/faq/codex/config-toml)
- [O que são model_provider, base_url e wire_api](/pt-br/faq/codex/model-provider-base-url-wire-api)
- [O que significam review_model e reasoning_effort](/pt-br/faq/codex/review-model-reasoning-effort)
- [O que é AGENTS.md e como escrever esse arquivo](/pt-br/faq/codex/agents-md)
- [O que são os modos sandbox e approval do Codex CLI](/pt-br/faq/codex/sandbox-approval)
- [Como manter o login oficial do Codex com uma API de terceiros](/pt-br/faq/codex/official-login-third-party-api)
- [API compatível com OpenAI ou API compatível com Anthropic](/pt-br/faq/concepts/openai-compatible-vs-anthropic-compatible)
- [Claude Code ou Codex CLI](/pt-br/faq/concepts/claude-code-vs-codex-cli)
- [Gerenciar o provedor do Codex com o CC Switch](/pt-br/ai-tools/cc-switch#codex-cli)

### Referência oficial

- [Referência de configuração do Codex](https://developers.openai.com/codex/config-reference)
- [Referência oficial de autenticação](https://developers.openai.com/codex/auth)

## Detalhes técnicos

<Accordion title="Protocolo Responses">
  O Codex usa OpenAI Responses. Defina `base_url` como `{{openai-base-url}}` e mantenha `wire_api = "responses"`. O Codex acrescenta `/responses`.
</Accordion>
