---
title: "OpenAI Responses API"
description: "测试 BetterToken 面向 Codex 的 OpenAI Responses API：Base URL、Bearer API Key 与 POST /v1/responses。"
openapi: "api-reference/openapi.json POST /v1/responses"
---

`POST /v1/responses`

适用于 Codex 与使用 OpenAI Responses API 的应用。

<Note>
  Base URL：`{{openai-base-url}}`。通过 `Authorization: Bearer YOUR_API_KEY` 传入 API Key。
</Note>

## 在 Playground 中测试

在右侧 Playground 填入你的 API Key；如有需要，将 `model` 改为当前可用的 GPT 提供商模型 ID，然后发送请求。测试前请在<a href={"{{model-plaza-url}}"}>模型广场</a>确认模型。

<Warning>
  只使用你自己的测试 API Key。不要把 API Key 写入代码、截图、工单或 Git 仓库。
</Warning>

## 相关接口

- [OpenAI Chat Completions API](/zh/api-reference/chat-completions)
- [Anthropic Messages API](/zh/api-reference/messages)
