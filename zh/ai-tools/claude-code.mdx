---
title: "Claude Code 安装、Claude 模型 API Key 与 Base URL 配置"
sidebarTitle: "Claude Code"
description: "安装 Claude Code，使用命令行或配置文件填写 BetterToken API Key、Base URL，并为 Kimi、GLM 配置模型映射。"
icon: "terminal"
---

Claude 提供商可以通过命令行或配置文件直接接入 Claude Code。Kimi、GLM 使用相同 Base URL，并在配置文件中填写模型映射。GPT 需要通过[进阶配置](#进阶配置)接入。

## 重点信息

| 字段 | 填写内容 |
| --- | --- |
| API Key | BetterToken API Key |
| Base URL | `{{anthropic-base-url}}` |
| Model | 由 Claude Code 选择 |

## 准备工作

- 安装最新版 [Claude Code](https://docs.anthropic.com/en/docs/claude-code)
- 安装 Node.js
- <a href={"{{register-url}}"}>创建并复制 BetterToken API Key</a>

## 安装

```bash
npm install -g @anthropic-ai/claude-code
```

## 命令行配置

BetterToken Automatic setup 脚本会写入 Claude Code provider 配置。脚本需要 Node.js；如果没有通过参数提供 API Key，运行时会交互式提示输入。

<Tabs>
  <Tab title="macOS / Linux">
    ```bash
    curl -fsSL "{{install-claudecode-provider-sh-url}}" | bash
    ```
  </Tab>
  <Tab title="Windows">
    ```powershell
    iwr "{{install-claudecode-provider-ps1-url}}" -OutFile "$env:TEMP\install-claudecode-provider.ps1"; powershell -ExecutionPolicy Bypass -File "$env:TEMP\install-claudecode-provider.ps1"
    ```
  </Tab>
</Tabs>

运行完成后检查 `~/.claude/settings.json`（Windows 为 `%USERPROFILE%\.claude\settings.json`），然后完全重启 Claude Code。希望逐项控制配置时，使用下面的手动配置。

## 手动配置

### 配置 Claude 提供商

Claude Code CLI 的配置文件位置：

| 系统 | 文件 |
| --- | --- |
| macOS / Linux | `~/.claude/settings.json` |
| Windows | `%USERPROFILE%\.claude\settings.json` |

写入以下配置：

```json
{
  "env": {
    "ANTHROPIC_BASE_URL": "{{anthropic-base-url}}",
    "ANTHROPIC_AUTH_TOKEN": "YOUR_API_KEY",
    "CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC": "1",
    "API_TIMEOUT_MS": "3000000"
  }
}
```

Claude 提供商只需要上面四个变量。不要额外设置 `ANTHROPIC_MODEL` 或 `ANTHROPIC_DEFAULT_*_MODEL`。

### 配置 Kimi 或 GLM 提供商

Kimi 和 GLM 需要在同一个 `settings.json` 中增加模型映射。把每个 `YOUR_MODEL_ID` 替换为 Setup 弹窗或模型广场中的实际 Model ID：

```json
{
  "env": {
    "ANTHROPIC_BASE_URL": "{{anthropic-base-url}}",
    "ANTHROPIC_AUTH_TOKEN": "YOUR_API_KEY",
    "CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC": "1",
    "API_TIMEOUT_MS": "3000000",
    "ANTHROPIC_DEFAULT_HAIKU_MODEL": "YOUR_MODEL_ID",
    "ANTHROPIC_DEFAULT_SONNET_MODEL": "YOUR_MODEL_ID",
    "ANTHROPIC_DEFAULT_OPUS_MODEL": "YOUR_MODEL_ID"
  }
}
```

三个映射可以使用同一个 Model ID，也可以分别使用同一提供商下的不同模型。不要把 GPT Model ID 填入这里；GPT 使用页面末尾的进阶配置。

### 在 VS Code Claude Code 扩展中配置

扩展使用 VS Code 的 `settings.json`。添加独立的 `claudeCode.environmentVariables` 配置：

```json
{
  "claudeCode.environmentVariables": [
    {
      "name": "ANTHROPIC_BASE_URL",
      "value": "{{anthropic-base-url}}"
    },
    {
      "name": "ANTHROPIC_AUTH_TOKEN",
      "value": "YOUR_API_KEY"
    },
    {
      "name": "CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC",
      "value": "1"
    },
    {
      "name": "API_TIMEOUT_MS",
      "value": "3000000"
    }
  ]
}
```

保存后执行 **Developer: Reload Window**，或完全退出并重启 VS Code。

## 验证连接

进入项目目录并启动：

```bash
claude
```

发送一条简短消息。能够正常返回内容，且没有 `401`、连接失败或模型不存在错误，即表示配置成功。修改配置后需要完全重启 Claude Code。

## 切换模型

- Claude 提供商：由 Claude Code 管理模型选择，不要添加 `ANTHROPIC_MODEL` 或 `ANTHROPIC_DEFAULT_*_MODEL`。
- Kimi 或 GLM 提供商：修改 `ANTHROPIC_DEFAULT_HAIKU_MODEL`、`ANTHROPIC_DEFAULT_SONNET_MODEL` 和 `ANTHROPIC_DEFAULT_OPUS_MODEL`，保存后完全重启 Claude Code。
- GPT 提供商：按照下面的[进阶配置](#进阶配置)切换模型。

## 常见错误

| 错误 | 处理方式 |
| --- | --- |
| `401` 或认证失败 | 检查 `ANTHROPIC_AUTH_TOKEN`，确认没有空格或过期 Key。 |
| `ConnectionRefused` | 确认 `ANTHROPIC_BASE_URL` 正好是 `{{anthropic-base-url}}`，不要添加 `/v1`。 |
| 模型不存在 | Claude 提供商不要手动填写模型；Kimi 或 GLM 请检查三个 `ANTHROPIC_DEFAULT_*_MODEL` 是否使用了当前有效的 Model ID。 |
| 配置未生效 | 检查文件路径，清除冲突的环境变量，然后完全重启 Claude Code。 |

## 进阶配置

{/* tool-provider-support:start */}
### 支持的提供商

| 提供商 | 状态 |
| --- | --- |
| Claude | 命令行配置 + 手动配置 |
| GPT | 进阶配置 |
| Kimi | 手动配置 |
| GLM | 手动配置 |

<Note>状态仅针对本页介绍的 BetterToken 接入方式。</Note>

<Accordion title="配置方式说明">
  - **进阶配置**：安装 CC Switch，并使用本地代理、协议转换或模型映射。
  - **手动配置**：填写 API Key、Base URL 和 Model。
  - **命令行配置 + 手动配置**：可以使用生成的一键命令，也可以按照完整步骤手动配置。
</Accordion>
{/* tool-provider-support:end */}

需要在 Claude Code 中使用 GPT 时，请通过 [CC Switch 配置 BetterToken](/zh/ai-tools/cc-switch#claude-code)。在 Claude Code Tab 中填写 API Key、Base URL 和 Model Mapping，并按说明开启代理。

### 相关 FAQ

- [Claude Code 是什么？它是如何工作的？](/zh/faq/claude-code/what-is-claude-code)
- [Claude Code 是用 Claude.ai 账号还是 API Key？](/zh/faq/claude-code/claude-account-or-api-key)
- [如何在 Claude Code 中配置 API Key 和 Base URL？](/zh/faq/claude-code/api-key-base-url)
- [Kimi K3 如何接入 Claude Code？](/zh/faq/claude-code/kimi-k3-api)
- [OpenAI-compatible API 和 Anthropic-compatible API 有什么区别？](/zh/faq/concepts/openai-compatible-vs-anthropic-compatible)
- [Claude Code 中的 CLAUDE.md 是什么？应该怎么写？](/zh/faq/claude-code/claude-md)
- [Claude Code token 为什么消耗很多？如何降低成本？](/zh/faq/token-cost/claude-code-token-usage)
- [Claude Code 和 Cursor 哪个更适合本地开发？](/zh/faq/claude-code/claude-code-vs-cursor)
- [Claude Code 和 Codex CLI 有什么区别？](/zh/faq/concepts/claude-code-vs-codex-cli)
- [如何在 Claude Code 中切换 Sonnet 和 Opus？](/zh/faq/claude-code/switch-sonnet-opus)
- [Claude Code 权限和 hooks 是什么？](/zh/faq/claude-code/permissions-and-hooks)
- [MCP 和 API Key / Base URL 有什么区别？](/zh/faq/concepts/mcp-vs-api-key-base-url)

## 技术说明

<Accordion title="协议与 endpoint">
  Claude Code 使用 Anthropic Messages。`ANTHROPIC_BASE_URL` 填写 `{{anthropic-base-url}}`，客户端会自行请求 `/v1/messages`，因此不要在 Base URL 后添加 `/v1`。
</Accordion>
