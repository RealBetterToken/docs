---
title: "Codex CLI/App 自定义 provider：config.toml、API Key 与 Base URL"
sidebarTitle: "Codex"
description: "使用 config.toml、auth.json、BetterToken API Key 和 Responses Base URL 配置 Codex CLI 与 Codex App。"
icon: "code"
---

Codex CLI 和 Codex App 的本地任务共用 `config.toml` 与 `auth.json`，通过 OpenAI Responses 使用 BetterToken API Key。

## 重点信息

| 字段 | 填写内容 |
| --- | --- |
| API Key | `auth.json` → `OPENAI_API_KEY` |
| Base URL | `{{openai-base-url}}` |
| Model | `gpt-6-astra` |

## 准备工作

- 安装 Node.js
- <a href={"{{register-url}}"}>创建 BetterToken API Key</a>
- 从<a href={"{{model-plaza-url}}"}>模型广场</a>或 Key 的 **Setup** 弹窗复制 Model ID

## 安装

```bash
npm install -g @openai/codex
```

## 确认路径并备份

如果设置了自定义 `CODEX_HOME`，请编辑该目录中的 `config.toml` 和 `auth.json`。否则，macOS / Linux 使用 `~/.codex`，Windows 使用 `%USERPROFILE%\.codex`。CLI 与 App 必须使用同一目录。WSL 有独立的用户目录。

修改前完全退出 Codex，将两个文件备份到私密位置。文件不存在时可创建。替换已有设置，不要重复追加键或表；保留无关配置。

<Warning>
  此配置会从 ChatGPT 登录切换为 API Key 认证，不保留官方账号访问状态。妥善保护 `auth.json` 及其备份，不要提交到代码仓库或分享。
</Warning>

## 命令行配置

BetterToken Automatic setup 脚本会写入 Codex provider 配置。脚本需要 Node.js；如果没有通过参数提供 API Key 或 Model ID，运行时会交互式提示输入。

<Tabs>
  <Tab title="macOS / Linux">
    ```bash
    curl -fsSL "{{install-codex-provider-sh-url}}" | bash
    ```
  </Tab>
  <Tab title="Windows">
    ```powershell
    iwr "{{install-codex-provider-ps1-url}}" -OutFile "$env:TEMP\install-codex-provider.ps1"; powershell -ExecutionPolicy Bypass -File "$env:TEMP\install-codex-provider.ps1"
    ```
  </Tab>
</Tabs>

安装后，按照下方手动配置检查两个文件，修正不一致的设置后再启动 Codex。

## 手动配置

### 编辑 config.toml

将 `model_provider`、`model` 和 `cli_auth_credentials_store` 放在所有 TOML 表之前的顶层，不要放在 profile 中。选中的提供商 ID 必须与表名一致。使用本页文件认证时，移除该提供商下的 `env_key` 和 `experimental_bearer_token`。

```toml
model_provider = "bettertoken"
model = "gpt-6-astra"
cli_auth_credentials_store = "file"

[model_providers.bettertoken]
name = "BetterToken"
base_url = "{{openai-base-url}}"
wire_api = "responses"
requires_openai_auth = true
request_max_retries = 4
stream_max_retries = 8
stream_idle_timeout_ms = 300000
supports_websockets = false
```

示例使用 GPT-6 Astra（`gpt-6-astra`）。切换模型时，从<a href={"{{model-plaza-url}}"}>模型广场</a>复制完整 ID 并替换 `model`。GPT、Kimi 和 GLM 使用相同的文件认证结构；自动配置适用于 GPT。

### 编辑 auth.json

`auth.json` 与 `config.toml` 位于同一目录。如果找不到 `auth.json`，请在该目录中新建这个文件。将文件内容设为以下 JSON，把 `YOUR_API_KEY` 替换为你的 BetterToken API Key，然后保存。

```json
{
  "auth_mode": "apikey",
  "OPENAI_API_KEY": "YOUR_API_KEY"
}
```

## 验证连接

保存两个文件后，在终端直接启动新的 Codex CLI 会话：

```bash
codex
```

完全退出 Codex App，包括后台进程，然后重新打开并新建本地任务。在实际使用的每个客户端发送简短提示词，确认能够正常返回。

## 切换模型

修改 `model` 和可选的 `review_model`，保存后结束当前 Codex 进程并重新启动。模型 ID 以 Setup 弹窗或模型广场为准。

## 常见错误

出现 `401`、`403` 或登录提示时，检查实际使用的目录、`auth.json` 中的 Key、`auth_mode = apikey`、`cli_auth_credentials_store = file` 和 `requires_openai_auth = true`。提供商或模型报错时，检查提供商 ID 是否一致、`{{openai-base-url}}`、`wire_api = responses` 及模型是否可用。配置未生效时，检查覆盖设置，并确认 CLI 与 App 使用相同的 `CODEX_HOME`。

## 进阶配置

{/* tool-provider-support:start */}
### 支持的提供商

| 提供商 | 状态 |
| --- | --- |
| Claude | 暂不支持 |
| GPT | 命令行配置 + 手动配置 |
| Kimi | 手动配置 |
| GLM | 手动配置 |

<Note>状态仅针对本页介绍的 BetterToken 接入方式。</Note>

<Accordion title="配置方式说明">
  - **命令行配置 + 手动配置**：可以使用生成的一键命令，也可以按照完整步骤手动配置。
  - **手动配置**：填写 API Key、Base URL 和 Model。
  - **不支持**：当前没有经过验证的直接配置方式。
</Accordion>
{/* tool-provider-support:end */}

### Codex Extension

Codex VS Code Extension 与 CLI/App 共享 `config.toml`，但保留官方登录时使用不同鉴权配置。请查看 [Codex Extension 配置指南](/zh/ai-tools/codex-vscode)。

### 相关 FAQ

- [Codex CLI 是什么？](/zh/faq/codex/what-is-codex-cli)
- [Codex CLI、Codex App 和 Codex Web 有什么区别？](/zh/faq/codex/codex-cli-vs-app-vs-web)
- [Codex config.toml 配置指南](/zh/faq/codex/config-toml)
- [model_provider、base_url 和 wire_api 是什么？](/zh/faq/codex/model-provider-base-url-wire-api)
- [review_model 和 reasoning_effort 是什么？](/zh/faq/codex/review-model-reasoning-effort)
- [AGENTS.md 是什么？应该怎么写？](/zh/faq/codex/agents-md)
- [Codex CLI sandbox 和 approval mode 是什么？](/zh/faq/codex/sandbox-approval)
- [使用第三方 API 时如何保留 Codex 官方登录？](/zh/faq/codex/official-login-third-party-api)
- [OpenAI-compatible API 和 Anthropic-compatible API 有什么区别？](/zh/faq/concepts/openai-compatible-vs-anthropic-compatible)
- [Claude Code 和 Codex CLI 有什么区别？](/zh/faq/concepts/claude-code-vs-codex-cli)
- [使用 CC Switch 管理 Codex provider](/zh/ai-tools/cc-switch#codex-cli)

### 官方参考

- [Codex 配置参考](https://developers.openai.com/codex/config-reference)
- [官方认证参考](https://developers.openai.com/codex/auth)

## 技术说明

<Accordion title="Responses 协议">
  Codex 使用 OpenAI Responses。`base_url` 填写 `{{openai-base-url}}`，`wire_api` 固定为 `responses`。Codex 会自行请求 `/responses`。
</Accordion>
