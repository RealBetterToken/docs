---
title: "Kilo Code 安装、API Key、Base URL 与自定义模型配置"
sidebarTitle: "Kilo Code"
description: "安装 Kilo Code，配置 BetterToken Base URL 和 API Key，选择兼容的提供商模型并排查常见错误。"
icon: "puzzle-piece"
---

要在 Kilo Code 中使用 BetterToken，请准备 API Key，填写下表中的 Base URL，并从模型广场选择当前可用的 Model ID。

## 三步完成首次请求

1. 获取 BetterToken API Key，并从<a href={"{{model-plaza-url}}"}>模型广场</a>选择 Model ID。
2. 在 Kilo Code 中选择 **OpenAI Compatible**，填写 API Key、`{{openai-base-url}}` 和 Model ID。
3. 保存配置，重启扩展，然后在新对话中发送一个简短请求。

## 重点信息

| 字段 | 填写内容 |
| --- | --- |
| API Key | BetterToken API Key |
| Base URL | `{{openai-base-url}}` |
| Model | 从<a href={"{{model-plaza-url}}"}>模型广场</a>复制的当前 Model ID |

## 准备工作

- 已安装该工具的当前版本
- BetterToken API Key：<a href={"{{register-url}}"}>注册并获取</a>
- 从<a href={"{{model-plaza-url}}"}>模型广场</a>复制的 Model ID

### 工具特定要求

- 已安装 VS Code

## 安装

如果已经安装 Kilo Code，请直接进入[手动配置](#手动配置)，填写 BetterToken API Key、Base URL 和 Model ID。

如果尚未安装扩展，或需要了解安装与俄罗斯地区使用背景，请先阅读[英文 Kilo Code 安装与自定义 API 指南](https://bettertoken.ai/en/blog/kilo-code-russia-api-setup/)，再回到本页使用当前配置字段。

## 手动配置

主要通过 Kilo Code 的设置界面完成以下配置，不需要手动编辑配置文件。

| 字段 | 填写内容 |
| --- | --- |
| API Key | BetterToken API Key |
| Base URL | `{{openai-base-url}}` |
| Model | 从<a href={"{{model-plaza-url}}"}>模型广场</a>复制的当前 Model ID |

### 配置步骤

<Steps>
  <Step title="清除冲突的环境变量">
    ```bash
    unset OPENAI_API_KEY
    unset OPENAI_BASE_URL
    ```
  </Step>

  <Step title="打开 Kilo Code 设置">
    打开 Kilo Code 的设置界面。
  </Step>

  <Step title="配置 API 提供商">
    按以下方式填写：

    - **API Provider** → `OpenAI Compatible`
    - **Base URL** → `{{openai-base-url}}`
    - **API Key** → 你的 BetterToken API Key
    - **Model** → 从<a href={"{{model-plaza-url}}"}>模型广场</a>复制的 **受支持提供商的模型 ID**
  </Step>

  <Step title="仅在官方文档明确要求时再配置额外 headers">
    我们查到的 Kilo Code 官方资料提到 OpenAI-compatible provider 支持可选自定义 headers，但没有把 `User-Agent` 列为 BetterToken 默认接入的必填项。

    如果你还接了自定义网关、租户路由或审计服务，再按对应上游服务的官方文档补充所需 headers。
  </Step>

  <Step title="重启 Kilo Code">
    修改配置后，请重启 VS Code 或重新加载 Kilo Code 插件，使新配置生效。
  </Step>
</Steps>

<Note>
  如需额外 headers，请以 Kilo Code 官方当前版本说明或上游网关要求为准，不要默认把 `User-Agent` 当作 BetterToken 必填项。
</Note>

## 验证连接

打开一个新的 Kilo Code 对话，发送简短请求，例如：`只回复一个词：OK`。如果工具正常返回内容，并且没有出现鉴权或 Model ID 错误，说明首次请求成功。修改配置后请完全重启 VS Code 或扩展。遇到问题时直接查看[常见错误](#常见错误)。

## 切换模型

打开模型选择器，或修改配置中的 `Model` 字段。请填写<a href={"{{model-plaza-url}}"}>模型广场</a>中的完整 Model ID，然后重新启动当前会话。

## 常见错误

| 错误 | 排查方式 |
| --- | --- |
| `401` 或 `Unauthorized` | 检查 BetterToken API Key，并删除首尾空格。 |
| `404` | 检查 Base URL，不要附加具体 endpoint 路径。 |
| `model not found` | 从模型广场重新复制当前 Model ID。 |
| 配置未生效 | 保存配置，并完全退出后重新启动工具。 |


- [OpenAI-compatible API 和 Anthropic-compatible API 有什么区别？](/zh/faq/concepts/openai-compatible-vs-anthropic-compatible)
- [MCP 和 API Key / Base URL 有什么区别？](/zh/faq/concepts/mcp-vs-api-key-base-url)
- [Codex CLI 中 model_provider、base_url 和 wire_api 是什么？](/zh/faq/codex/model-provider-base-url-wire-api)
- [Cline 如何配置 OpenAI-compatible API？](/zh/faq/cline/openai-compatible-api)

## 进阶配置

{/* tool-provider-support:start */}
### 支持的提供商

| 提供商 | 状态 |
| --- | --- |
| Claude | 暂不支持 |
| GPT | 手动配置 |
| Kimi | 手动配置 |
| GLM | 手动配置 |

<Note>状态仅针对本页介绍的 BetterToken 接入方式。</Note>

<Accordion title="配置方式说明">
  - **手动配置**：填写 API Key、Base URL 和 Model。
  - **不支持**：当前没有经过验证的直接配置方式。
</Accordion>
{/* tool-provider-support:end */}

## 技术说明

<Accordion title="协议、endpoint 与 provider 内部字段">
  本页配置使用 `{{openai-base-url}}`。工具会自行拼接 OpenAI-compatible endpoint；除非具体字段明确要求，否则不要在 Base URL 后附加 `/chat/completions` 或 `/responses`。
</Accordion>
