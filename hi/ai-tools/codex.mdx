---
title: "Codex CLI/App custom प्रदाता: config.toml, API Key और Base URL"
sidebarTitle: "Codex"
description: "config.toml, auth.json, BetterToken API Key और Responses Base URL से Codex CLI और Codex App सेट करें।"
icon: "code"
---

स्थानीय कार्यों के लिए Codex CLI और Codex App एक ही `config.toml` और `auth.json` इस्तेमाल करते हैं। यह सेटअप OpenAI Responses के ज़रिए BetterToken API Key उपयोग करता है।

## मुख्य सेटिंग

| Field | मान |
| --- | --- |
| API Key | `auth.json` → `OPENAI_API_KEY` |
| Base URL | `{{openai-base-url}}` |
| Model | `gpt-6-astra` |

## आवश्यकताएं

- Node.js इंस्टॉल करें
- <a href={"{{register-url}}"}>BetterToken API Key बनाएं</a>
- <a href={"{{model-plaza-url}}"}>मॉडल प्लाज़ा</a> या Key **Setup** dialog से Model ID कॉपी करें

## इंस्टॉल करें

```bash
npm install -g @openai/codex
```

## फ़ाइलें खोजें और बैकअप लें

यदि आपने अपना `CODEX_HOME` सेट किया है, तो उसी डायरेक्टरी में `config.toml` और `auth.json` संपादित करें। अन्यथा macOS / Linux पर `~/.codex` या Windows पर `%USERPROFILE%\.codex` उपयोग करें। CLI और App को एक ही डायरेक्टरी इस्तेमाल करनी चाहिए। WSL की उपयोगकर्ता डायरेक्टरी अलग होती है।

संपादन से पहले Codex पूरी तरह बंद करें। दोनों फ़ाइलों का बैकअप निजी स्थान पर रखें। जो फ़ाइल मौजूद नहीं है उसे बनाएँ। मौजूदा सेटिंग बदलें; कुंजियाँ या टेबल दोबारा न जोड़ें। असंबंधित सेटिंग बनाए रखें।

<Warning>
  यह सेटअप ChatGPT लॉगिन से API Key प्रमाणीकरण पर स्विच करता है। आधिकारिक खाते का एक्सेस बना नहीं रहता। `auth.json` और उसके बैकअप सुरक्षित रखें; उन्हें साझा या रिपॉज़िटरी में कमिट न करें।
</Warning>

## Command-line सेटअप

BetterToken automatic setup script Codex प्रदाता configuration लिखता है। इसके लिए Node.js आवश्यक है। API Key या Model ID argument में न देने पर script उन्हें दर्ज करने को कहता है।

<Tabs>
  <Tab title="macOS / Linux">
    ```bash
    curl -fsSL "{{install-codex-provider-sh-url}}" | bash
    ```
  </Tab>
  <Tab title="Windows">
    ```powershell
    iwr "{{install-codex-provider-ps1-url}}" -OutFile "$env:TEMP\install-codex-provider.ps1"; powershell -ExecutionPolicy Bypass -File "$env:TEMP\install-codex-provider.ps1"
    ```
  </Tab>
</Tabs>

इंस्टॉल करने के बाद दोनों फ़ाइलों का नीचे दिए गए मैन्युअल सेटअप से मिलान करें और Codex शुरू करने से पहले अंतर ठीक करें।

## मैन्युअल सेटअप

### config.toml संपादित करें

`model_provider`, `model` और `cli_auth_credentials_store` को सभी TOML टेबल से पहले शीर्ष स्तर पर रखें, प्रोफ़ाइल के अंदर नहीं। चुने गए प्रदाता का ID उसकी टेबल के नाम से मेल खाना चाहिए। इस फ़ाइल-आधारित प्रमाणीकरण के लिए चुने गए प्रदाता से `env_key` और `experimental_bearer_token` हटाएँ।

```toml
model_provider = "bettertoken"
model = "gpt-6-astra"
cli_auth_credentials_store = "file"

[model_providers.bettertoken]
name = "BetterToken"
base_url = "{{openai-base-url}}"
wire_api = "responses"
requires_openai_auth = true
request_max_retries = 4
stream_max_retries = 8
stream_idle_timeout_ms = 300000
supports_websockets = false
```

इस उदाहरण में GPT-6 Astra (`gpt-6-astra`) उपयोग होता है। दूसरे मॉडल के लिए <a href={"{{model-plaza-url}}"}>मॉडल प्लाज़ा</a> से पूरा ID कॉपी करके `model` बदलें। GPT, Kimi और GLM एक ही फ़ाइल-आधारित प्रमाणीकरण संरचना इस्तेमाल करते हैं; स्वचालित सेटअप GPT के लिए उपलब्ध है।

### auth.json संपादित करें

`auth.json` और `config.toml` एक ही डायरेक्टरी में होते हैं। अगर `auth.json` नहीं है, तो उसी डायरेक्टरी में यह फ़ाइल बनाएँ। फ़ाइल की सामग्री के रूप में निम्न JSON लिखें, `YOUR_API_KEY` की जगह अपनी BetterToken API Key डालें और फ़ाइल सेव करें।

```json
{
  "auth_mode": "apikey",
  "OPENAI_API_KEY": "YOUR_API_KEY"
}
```

## कनेक्शन सत्यापित करें

दोनों फ़ाइलें सहेजें। टर्मिनल में नया Codex CLI सत्र सीधे शुरू करें:

```bash
codex
```

पृष्ठभूमि प्रक्रियाओं सहित Codex App पूरी तरह बंद करें। फिर खोलें और नया स्थानीय कार्य बनाएँ। अपने इस्तेमाल के हर क्लाइंट में छोटा प्रॉम्प्ट भेजें। सामान्य उत्तर उस क्लाइंट का कनेक्शन सत्यापित करता है।

## मॉडल बदलें

`model` बदलें और जरूरत हो तो `review_model` भी बदलें। File save करें, मौजूदा Codex process रोकें और दोबारा शुरू करें। Setup dialog या मॉडल प्लाज़ा का Model ID उपयोग करें।

## सामान्य त्रुटियां

`401`, `403` या लॉगिन अनुरोध आने पर सक्रिय डायरेक्टरी, `auth.json` की Key, `auth_mode = apikey`, `cli_auth_credentials_store = file` और `requires_openai_auth = true` जाँचें। प्रदाता या मॉडल की त्रुटि होने पर प्रदाता ID का मिलान, `{{openai-base-url}}`, `wire_api = responses` और मॉडल की उपलब्धता जाँचें। बदलाव लागू न हों तो कॉन्फ़िगरेशन ओवरराइड जाँचें और सुनिश्चित करें कि CLI और App एक ही `CODEX_HOME` इस्तेमाल कर रहे हैं।

## उन्नत सेटअप

{/* tool-provider-support:start */}
### समर्थित provider

| Provider | स्थिति |
| --- | --- |
| Claude | समर्थित नहीं |
| GPT | कमांड लाइन + मैन्युअल सेटअप |
| Kimi | मैन्युअल सेटअप |
| GLM | मैन्युअल सेटअप |

<Note>ये स्थितियां केवल इस पेज पर बताए गए BetterToken सेटअप पर लागू होती हैं।</Note>

<Accordion title="सेटअप के तरीकों का अर्थ">
  - **कमांड लाइन + मैन्युअल सेटअप**: तैयार कमांड का उपयोग करें या पूरे मैन्युअल चरणों का पालन करें।
  - **मैन्युअल सेटअप**: API Key, Base URL और Model दर्ज करें।
  - **समर्थित नहीं**: सीधे सेटअप का कोई सत्यापित तरीका अभी उपलब्ध नहीं है।
</Accordion>
{/* tool-provider-support:end */}

### Codex Extension

Codex VS Code Extension `config.toml` साझा करता है, लेकिन official login बनाए रखने के लिए अलग authentication configuration चाहिए। [Codex Extension सेटअप गाइड](/hi/ai-tools/codex-vscode) देखें।

### संबंधित FAQ

- [Codex CLI क्या है?](/hi/faq/codex/what-is-codex-cli)
- [Codex CLI, Codex App और Codex Web की तुलना](/hi/faq/codex/codex-cli-vs-app-vs-web)
- [Codex config.toml सेटअप गाइड](/hi/faq/codex/config-toml)
- [model_provider, base_url और wire_api क्या हैं?](/hi/faq/codex/model-provider-base-url-wire-api)
- [review_model और reasoning_effort का क्या अर्थ है?](/hi/faq/codex/review-model-reasoning-effort)
- [AGENTS.md क्या है और इसे कैसे लिखें?](/hi/faq/codex/agents-md)
- [Codex CLI sandbox और approval mode क्या हैं?](/hi/faq/codex/sandbox-approval)
- [Third-party API के साथ official Codex login बनाए रखें](/hi/faq/codex/official-login-third-party-api)
- [OpenAI-compatible API और Anthropic-compatible API की तुलना](/hi/faq/concepts/openai-compatible-vs-anthropic-compatible)
- [Claude Code और Codex CLI की तुलना](/hi/faq/concepts/claude-code-vs-codex-cli)
- [CC Switch से Codex प्रदाता प्रबंधित करें](/hi/ai-tools/cc-switch#codex-cli)

### Official संदर्भ

- [Codex configuration संदर्भ](https://developers.openai.com/codex/config-reference)
- [आधिकारिक प्रमाणीकरण संदर्भ](https://developers.openai.com/codex/auth)

## तकनीकी विवरण

<Accordion title="Responses protocol">
  Codex OpenAI Responses उपयोग करता है। `base_url` को `{{openai-base-url}}` पर सेट करें और `wire_api = "responses"` बनाए रखें। Codex `/responses` जोड़ता है।
</Accordion>
