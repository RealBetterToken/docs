---
title: "Cline 接入 BetterToken 指南"
sidebarTitle: "Cline"
description: "在 Cline 中使用 BetterToken，接入 GPT 分组模型。"
icon: "c"
---

Cline 是 VS Code 的 AI 编程助手插件。接入 BetterToken 时，请选择 OpenAI Compatible 提供商，并使用 GPT 分组模型 ID。

## 前置条件

- 已安装 VS Code
- 已获取 BetterToken API Key（<a href={"{{register-url}}"}>注册并获取</a>）

## 安装 Cline

打开 VS Code，在扩展市场搜索 **Cline**，点击 Install 安装。

## 配置步骤

<Steps>
  <Step title="清除冲突的环境变量">
    ```bash
    unset OPENAI_API_KEY
    unset OPENAI_BASE_URL
    ```
  </Step>

  <Step title="打开 Cline 配置界面">
    点击 Cline 侧边栏中的 **Use your own API key**。
  </Step>

  <Step title="配置 API 提供商">
    按以下方式填写：

    - **API Provider** → `OpenAI Compatible`
    - **Base URL** → `{{openai-base-url}}`
    - **API Key** → 你的 BetterToken API Key
    - **Model** → 从<a href={"{{model-plaza-url}}"}>模型广场</a>复制的 **GPT 分组模型 ID**
  </Step>

  <Step title="仅在官方文档明确要求时再配置额外 headers">
    我们查到的 Cline 官方 OpenAI Compatible 接入文档默认只要求 `Base URL`、API Key 和模型 ID。接入 BetterToken 时通常不需要额外设置 `User-Agent`。

    如果你还接了自定义网关、租户路由或审计服务，再按对应上游服务的官方文档补充所需 headers。
  </Step>

  <Step title="保存配置">
    点击右上角 **Done** 保存配置。
  </Step>

  <Step title="重启 Cline">
    修改配置后，请重启 VS Code 或重新加载 Cline 插件，使新配置生效。
  </Step>
</Steps>

## 相关 FAQ

- [Cline 如何配置 OpenAI-compatible API？](/faq/cline/openai-compatible-api)
- [OpenAI-compatible API 和 Anthropic-compatible API 有什么区别？](/faq/concepts/openai-compatible-vs-anthropic-compatible)
- [MCP 和 API Key / Base URL 有什么区别？](/faq/concepts/mcp-vs-api-key-base-url)
- [Cursor 如何配置自定义 OpenAI API Key 和 Base URL？](/faq/cursor/custom-openai-api)

<Note>
  优先检查 `Base URL`、API Key、模型 ID 和工具版本；不要默认把 `User-Agent` 当作 BetterToken 必填项。
</Note>
