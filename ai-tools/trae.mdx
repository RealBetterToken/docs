---
title: "TRAE 接入 BetterToken 指南"
sidebarTitle: "TRAE"
description: "在 TRAE 中使用 BetterToken，接入 GPT 分组模型。"
icon: "bolt"
---

在 TRAE 中接入 BetterToken 时，请选择 OpenAI 类提供商，并使用 GPT 分组模型 ID。

## 前置条件

- 已获取 BetterToken API Key（<a href={"{{register-url}}"}>注册并获取</a>）

## 配置步骤

<Steps>
  <Step title="进入设置中心">
    在 **IDE** 模式界面中，点击界面右上角的 **设置** 图标，进入设置中心。

    你也可以在 **SOLO** 模式界面中，点击对话面板右上角的 **设置** 图标，进入设置中心。
  </Step>

  <Step title="打开模型面板">
    在左侧导航栏中，选择 **模型**，进入 **模型** 面板。
  </Step>

  <Step title="添加模型">
    点击 **+ 添加模型** 按钮。

    ![TRAE 设置中心的模型面板，标出了设置入口、模型导航和添加模型按钮。](/images/trae/add-model.png)
  </Step>

  <Step title="填写模型配置">
    界面上显示 **添加模型** 窗口后，按下面的方式填写：

    | 配置项 | 值 |
    |--------|-----|
    | 服务商 | `OpenAI` |
    | 模型 | `自定义模型` |
    | 模型 ID | 从<a href={"{{model-plaza-url}}"}>模型广场</a>复制的 **GPT 分组模型 ID** |
    | API 密钥 | 从 BetterToken 控制台复制的 API Key |
    | 自定义请求地址 | `{{chat-completions-url}}` |

    ![TRAE 添加模型窗口，服务商选择 OpenAI，模型选择自定义模型，并填写模型 ID、API 密钥和自定义请求地址。](/images/trae/model-config.png)
  </Step>

  <Step title="保存并开始使用">
    点击 **确认** 保存配置，然后选择刚添加的 GPT 分组模型开始使用。
  </Step>
</Steps>

<Note>
  TRAE 接入 BetterToken 时，只有 **GPT 分组**模型支持使用。请不要填写 Claude 系列固定模型名，也不要把自定义请求地址写成 `{{openai-base-url}}`。
</Note>
