---
title: "Codex VS Code custom API: API Key и официальный вход"
sidebarTitle: "Codex VS Code Extension"
description: "Настройте Codex VS Code custom API через config.toml, Base URL и API Key BetterToken и сохраните официальный вход, плагины и Remote Control."
keywords:
  - "codex vscode custom api"
  - "codex vscode api key"
icon: "/icons/codex-vscode.svg"
---

Codex VS Code custom API использует общий с Codex CLI/App файл `~/.codex/config.toml`. API Key BetterToken можно передать через `experimental_bearer_token`, сохранив официальный вход, плагины и Remote Control.

## Codex VS Code custom API и API Key

Не вставляйте одновременно конфигурации CLI/App и Extension с одинаковым TOML table. Базовая настройка находится в [руководстве по Codex CLI/App](/ai-tools/codex), поля файла — в [FAQ по config.toml](/faq/codex/config-toml), а сохранение официальной авторизации — в [отдельной инструкции](/faq/codex/official-login-third-party-api).

## Ключевые параметры

| Поле | Значение |
| --- | --- |
| API Key | `experimental_bearer_token = "YOUR_API_KEY"` |
| Base URL | `{{openai-base-url}}` |
| Model | `gpt-6-astra` |

## Подготовка

- Установите последнюю версию Codex VS Code Extension
- Выполните официальный вход в Extension или Codex App
- <a href={"{{register-url}}"}>Создайте API Key BetterToken</a>
- Скопируйте Model ID из <a href={"{{model-plaza-url}}"}>model plaza</a> или окна **Setup** для ключа

## Установка

Если задан свой `CODEX_HOME`, редактируйте `config.toml` в этом каталоге. Иначе используйте `~/.codex/config.toml` в macOS / Linux или `%USERPROFILE%\.codex\config.toml` в Windows.

Полностью закройте Codex, затем создайте резервные копии существующих `config.toml` и `auth.json`. Обновите существующие настройки provider, не добавляя повторную таблицу provider.

Установите расширение из Marketplace VS Code, затем перезапустите VS Code.

## Ручная настройка

### Настройка Extension

Отредактируйте `~/.codex/config.toml`:

```toml
model_provider = "bettertoken"
model = "gpt-6-astra"

[model_providers.bettertoken]
name = "BetterToken"
base_url = "{{openai-base-url}}"
wire_api = "responses"
requires_openai_auth = true
experimental_bearer_token = "YOUR_API_KEY"
```

Сохраните файл, выполните **Developer: Reload Window** в VS Code и снова откройте панель Codex.

## Проверка подключения

Создайте новую сессию Extension и отправьте короткую задачу. Ответ от `gpt-6-astra` при доступных плагинах и Remote Control подтверждает настройку.

## Смена модели

Измените `model` и при необходимости `review_model`, затем выполните **Developer: Reload Window**. Используйте Model ID из окна Setup или model plaza.

## Частые ошибки

| Ошибка | Решение |
| --- | --- |
| Extension снова просит вход | Восстановите официальный вход и сохраните `requires_openai_auth = true`. |
| `401` или `403` | Скопируйте `experimental_bearer_token` заново; не перезаписывайте `auth.json`. |
| Duplicate table | Удалите второй `[model_providers.bettertoken]` и оставьте один режим авторизации. |
| Модель не найдена | Замените `gpt-6-astra` на актуальный полный ID. |
| Конфигурация не применилась | Сохраните файл и выполните **Developer: Reload Window**. |

## Расширенная настройка

{/* tool-provider-support:start */}
### Поддерживаемые провайдеры

| Провайдер | Статус |
| --- | --- |
| Claude | Не поддерживается |
| GPT | Ручная настройка |
| Kimi | Ручная настройка |
| GLM | Ручная настройка |

<Note>Статусы относятся к способу подключения BetterToken, описанному на этой странице.</Note>

<Accordion title="Что означают способы настройки">
  - **Ручная настройка**: укажите API Key, Base URL и Model.
  - **Не поддерживается**: проверенный способ прямого подключения пока отсутствует.
</Accordion>
{/* tool-provider-support:end */}

### Связанные материалы

- [Custom provider Codex CLI/App](/ai-tools/codex)
- [Сохранение официального входа Codex при стороннем API](/faq/codex/official-login-third-party-api)
- [Руководство по настройке Codex config.toml](/faq/codex/config-toml)
- [Что такое model_provider, base_url и wire_api?](/faq/codex/model-provider-base-url-wire-api)

### Официальная документация

- [Codex IDE extension](https://developers.openai.com/codex/ide)
- [Базовая конфигурация Codex](https://developers.openai.com/codex/config-basic)
- [Справочник конфигурации Codex](https://developers.openai.com/codex/config-reference)

## Технические детали

<Accordion title="Протокол Responses">
  Extension отправляет запросы модели на `{{openai-base-url}}` через OpenAI Responses.
</Accordion>
