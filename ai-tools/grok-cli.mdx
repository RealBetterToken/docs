---
title: "Grok CLI 接入 BetterToken 指南"
sidebarTitle: "Grok CLI"
description: "在 Grok CLI 中使用 BetterToken，接入 Codex 分组模型。"
icon: "g"
---

Grok CLI 通过环境变量配置 API 地址和 Key。接入 BetterToken 时，请使用 `https://www.bettertoken.ai/v1`，并指定 Codex 分组模型 ID。

<Warning>
  Grok CLI 更适合轻量场景。如需完整 Agent 工作流，优先使用 Claude Code、Codex 或 OpenClaw。
</Warning>

## 前置条件

- 已安装 Node.js
- 已获取 BetterToken API Key（[注册并获取](https://www.bettertoken.ai/register)）

## 安装 Grok CLI

```bash
npm install -g @vibe-kit/grok-cli
```

## 配置步骤

<Steps>
  <Step title="清除冲突的环境变量">
    ```bash
    unset OPENAI_API_KEY
    unset OPENAI_BASE_URL
    ```
  </Step>

  <Step title="设置 BetterToken 环境变量">
    ```bash
    export GROK_BASE_URL="https://www.bettertoken.ai/v1"
    export GROK_API_KEY="YOUR_API_KEY"
    ```
  </Step>

  <Step title="启动 Grok CLI 并指定模型">
    ```bash
    grok --model YOUR_CODEX_MODEL_ID
    ```

    将 `YOUR_CODEX_MODEL_ID` 替换为从[模型广场](https://www.bettertoken.ai/pricing)复制的 **Codex 分组模型 ID**。
  </Step>

  <Step title="重启 Grok CLI">
    修改环境变量后，请关闭当前 Grok CLI 会话，并重新启动命令行工具。
  </Step>
</Steps>

<Note>
  目前我们没有查到 Grok CLI 官方接入说明要求 BetterToken 额外配置 `User-Agent`。优先检查环境变量、模型 ID 和工具版本；只有当你额外接入的上游网关明确要求 headers 时，再按其文档补充。
</Note>
