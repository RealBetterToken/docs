---
title: "Configurer Hermes Agent : installation, API Key, Base URL et modèles personnalisés"
sidebarTitle: "Hermes Agent"
description: "Installez Hermes Agent, configurez la Base URL et l’API Key BetterToken, choisissez un modèle d’un fournisseur compatible et résolvez les erreurs courantes."
icon: "user-astronaut"
---

Pour connecter Hermes Agent à BetterToken, préparez une API Key, saisissez la Base URL ci-dessous et choisissez un Model ID actuel dans le model plaza.

## Paramètres principaux

| Champ | Valeur |
| --- | --- |
| API Key | BetterToken API Key |
| Base URL | `{{openai-base-url}}` |
| Model | Un Model ID actuel du <a href={"{{model-plaza-url}}"}>model plaza</a> |

## Prérequis

- La version actuelle de l’outil est installée
- Une BetterToken API Key : <a href={"{{register-url}}"}>inscrivez-vous pour en obtenir une</a>
- Un Model ID du <a href={"{{model-plaza-url}}"}>model plaza</a>

### Exigences supplémentaires

- Le Model ID sélectionné provient du **fournisseur compatible**
- macOS, Linux, Windows 10/11 ou WSL2
- Un modèle doté d’une fenêtre de contexte d’au moins 64K pour utiliser les outils

## Installation

### Installer Hermes Agent

<Tabs>
  <Tab title="Desktop (macOS / Windows)">
    Hermes recommande l’installateur Desktop sur macOS et Windows. [Téléchargez Hermes Desktop](https://hermes-agent.nousresearch.com/) et exécutez l’installateur correspondant à votre système.
  </Tab>

  <Tab title="Linux / macOS / WSL2">
    ```bash
    curl -fsSL https://hermes-agent.nousresearch.com/install.sh | bash
    ```
  </Tab>

  <Tab title="Windows PowerShell">
    Hermes Agent prend en charge nativement Windows 10 et 11. Exécutez ceci dans PowerShell ou Windows Terminal :

    ```powershell
    iex (irm https://hermes-agent.nousresearch.com/install.ps1)
    ```

    Aucun droit administrateur n’est requis. Ouvrez une nouvelle fenêtre PowerShell après l’installation.
  </Tab>
</Tabs>

```bash
hermes --version
hermes doctor
```

<Note>WSL2 reste compatible, mais il est facultatif. Utilisez-le si vous avez besoin d’un environnement Linux ou du terminal intégré au Web Dashboard.</Note>

### Référence rapide des fonctionnalités

| Fonctionnalité | Utilité |
| --- | --- |
| Terminal UI | Discutez directement avec l’agent dans votre terminal |
| Skills | Enregistrez des workflows réutilisables pour les tâches répétées |
| MCP | Connectez des capacités d’outils locales ou distantes |
| Scheduled tasks | Exécutez des tâches planifiées et des workflows récurrents |
| Subagents | Répartissez un travail complexe entre des agents spécialisés |
| Message gateway | Connectez l’agent à davantage de points d’entrée de messagerie |

## Configuration manuelle

Utilisez la configuration interactive de modèle Hermes Agent comme méthode principale. Modifiez directement les fichiers de configuration uniquement si vous avez besoin d’une configuration manuelle.

| Champ | Valeur |
| --- | --- |
| API Key | BetterToken API Key |
| Base URL | `{{openai-base-url}}` |
| Model | Un Model ID actuel du <a href={"{{model-plaza-url}}"}>model plaza</a> |

### Configuration

<Tabs>
  <Tab title="Interactive setup">
    <Steps>
      <Step title="Exécuter le sélecteur de modèle">
        Si un chat Hermes est ouvert, quittez d’abord la session. Exécutez ensuite :

        ```bash
        hermes model
        ```
      </Step>

      <Step title="Choisir Custom endpoint">
        Sélectionnez **Custom endpoint (self-hosted / VLLM / etc.)** dans la liste des fournisseurs.
      </Step>

      <Step title="Saisir la configuration BetterToken">
        Saisissez :

        - **Base URL**: `{{openai-base-url}}`
        - **API Key** : votre BetterToken API Key
        - **Model** : un **model ID d’un fournisseur compatible** copié depuis le <a href={"{{model-plaza-url}}"}>model plaza</a>

        Si Hermes demande le mode d’API, choisissez **Chat Completions**. Pour les outils de l’agent, utilisez un modèle avec au moins 64K de contexte.
      </Step>

      <Step title="Démarrer Hermes Agent">
        ```bash
        hermes --tui
        ```

        Exécutez `hermes` si vous préférez l’interface classique du terminal.
      </Step>
    </Steps>
  </Tab>

  <Tab title="Fichiers de configuration">
    Ajoutez le fournisseur custom dans `~/.hermes/config.yaml` :

    ```yaml
    model:
      default: YOUR_MODEL_ID
      provider: custom
      base_url: "{{openai-base-url}}"
    ```

    Stockez séparément la BetterToken API Key dans `~/.hermes/.env` :

    ```dotenv
    OPENAI_API_KEY=YOUR_API_KEY
    ```

    Remplacez `YOUR_MODEL_ID` par un model ID d’un fournisseur compatible du <a href={"{{model-plaza-url}}"}>model plaza</a> et `YOUR_API_KEY` par votre BetterToken API Key. Sous Windows natif, ces fichiers se trouvent dans `%LOCALAPPDATA%\hermes\`.
  </Tab>
</Tabs>

### Migrer depuis OpenClaw

Si vous utilisez déjà OpenClaw, vérifiez d’abord si votre version actuelle de Hermes Agent fournit la commande de migration. Exécutez un essai à blanc avant toute modification.

```bash
hermes claw migrate --dry-run
```

Après avoir confirmé la sortie, exécutez la migration :

```bash
hermes claw migrate
```

Après la migration, vérifiez encore la **Base URL**, l’API Key et le model ID de Hermes Agent. Ils doivent correspondre à `{{openai-base-url}}`, votre BetterToken API Key et un model ID d’un fournisseur compatible.

## Vérifier la connexion

Démarrez Hermes Agent et envoyez ce court prompt de test : `Reply with HERMES_OK only. Do not use tools.` Si la réponse est exactement `HERMES_OK`, la connexion fonctionne.

## Changer de modèle

En dehors d’une session, exécutez `hermes model`. Dans un chat actif, utilisez `/model` pour changer de fournisseur ou de modèle déjà configuré. Choisissez le Model ID exact du <a href={"{{model-plaza-url}}"}>model plaza</a>.

## Erreurs courantes

| Erreur | Vérifications |
| --- | --- |
| `401` ou `Unauthorized` | Vérifiez la BetterToken API Key et supprimez les espaces initiaux ou finaux. |
| `404` | Vérifiez la Base URL et n’ajoutez pas de chemin d’endpoint spécifique. |
| `model not found` | Copiez un Model ID actuel depuis le model plaza. |
| Les modifications ne sont pas appliquées | Enregistrez la configuration et redémarrez complètement l’outil. |


### Dépannage

- Si les Request échouent ou ne renvoient aucune réponse, vérifiez que la **Base URL** est exactement `{{openai-base-url}}`. N’ajoutez pas `/chat/completions` dans ce champ.
- Si Hermes indique que le modèle n’existe pas, vérifiez que **Model** est un model ID d’un fournisseur compatible du <a href={"{{model-plaza-url}}"}>model plaza</a>.
- Si l’authentification échoue, vérifiez les espaces copiés et confirmez que le Model ID sélectionné provient du **fournisseur compatible**.
- Si Hermes utilise toujours l’ancien modèle après une modification, quittez la session Hermes Agent actuelle et exécutez à nouveau `hermes`.
- BetterToken ne nécessite généralement pas de `User-Agent` supplémentaire ni d’en-têtes personnalisés. Ajoutez des en-têtes uniquement si votre passerelle personnalisée, le routage de locataire, la couche d’observabilité ou de cache les exige explicitement.

<Note>
  Ne validez pas d’API Key réelle dans votre dépôt. Pour une configuration partagée avec l’équipe, privilégiez les variables d’environnement ou une configuration locale.
</Note>

### FAQ associée

- [MCP, API Key et Base URL](/fr/faq/concepts/mcp-vs-api-key-base-url)
- [Que sont model_provider, base_url et wire_api ?](/fr/faq/codex/model-provider-base-url-wire-api)
- [Configurer une API OpenAI compatible dans Cline](/fr/faq/cline/openai-compatible-api)
- [Guide de configuration OpenClaw BetterToken](/fr/ai-tools/openclaw)

## Configuration avancée

{/* tool-provider-support:start */}
### Fournisseurs compatibles

| Fournisseur | État |
| --- | --- |
| Claude | Non compatible |
| GPT | Configuration manuelle |
| Kimi | Configuration manuelle |
| GLM | Configuration manuelle |

<Note>Les états concernent la méthode de configuration de BetterToken décrite sur cette page.</Note>

<Accordion title="Signification des méthodes de configuration">
  - **Configuration manuelle** : saisissez l’API Key, la Base URL et le Model.
  - **Non compatible** : aucune méthode de connexion directe vérifiée n’est disponible pour le moment.
</Accordion>
{/* tool-provider-support:end */}

## Détails techniques

<Accordion title="Protocole, endpoint et champs internes du fournisseur">
  Cette configuration utilise `{{openai-base-url}}`. L’outil ajoute le chemin d’endpoint OpenAI compatible. N’ajoutez pas `/chat/completions` ni `/responses` sauf si un champ spécifique l’exige explicitement.
</Accordion>
