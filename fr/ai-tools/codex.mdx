---
title: "Fournisseur personnalisé pour Codex CLI/App : config.toml, API Key et Base URL"
sidebarTitle: "Codex"
description: "Configurez Codex CLI et Codex App avec config.toml, auth.json, une BetterToken API Key et le Base URL Responses."
icon: "code"
---

Codex CLI et Codex App partagent `config.toml` et `auth.json` pour les tâches locales. Cette configuration utilise une BetterToken API Key via OpenAI Responses.

## Paramètres principaux

| Champ | Valeur |
| --- | --- |
| API Key | `auth.json` → `OPENAI_API_KEY` |
| Base URL | `{{openai-base-url}}` |
| Modèle | `gpt-6-astra` |

## Prérequis

- Installez Node.js
- <a href={"{{register-url}}"}>Créez une BetterToken API Key</a>
- Copiez un Model ID depuis le <a href={"{{model-plaza-url}}"}>model plaza</a> ou la boîte de dialogue **Setup** de l’API Key

## Installation

```bash
npm install -g @openai/codex
```

## Localisez les fichiers et sauvegardez-les

Si vous avez défini un `CODEX_HOME` personnalisé, modifiez `config.toml` et `auth.json` dans ce répertoire. Sinon, utilisez `~/.codex` sur macOS / Linux ou `%USERPROFILE%\.codex` sur Windows. CLI et App doivent utiliser le même répertoire. WSL possède son propre répertoire utilisateur.

Quittez complètement Codex avant toute modification. Sauvegardez les deux fichiers dans un emplacement privé. Créez les fichiers manquants. Remplacez les paramètres existants sans dupliquer les clés ni les tables ; conservez les autres paramètres.

<Warning>
  Cette configuration remplace la connexion ChatGPT par l’authentification avec une API Key. Elle ne conserve pas l’accès au compte officiel. Protégez `auth.json` et ses sauvegardes ; ne les partagez pas et ne les ajoutez pas au dépôt.
</Warning>

## Configuration par la ligne de commande

Le script de configuration automatique BetterToken écrit la configuration du fournisseur Codex. Il nécessite Node.js et demande l’API Key ou le Model ID s’ils ne sont pas transmis en arguments.

<Tabs>
  <Tab title="macOS / Linux">
    ```bash
    curl -fsSL "{{install-codex-provider-sh-url}}" | bash
    ```
  </Tab>
  <Tab title="Windows">
    ```powershell
    iwr "{{install-codex-provider-ps1-url}}" -OutFile "$env:TEMP\install-codex-provider.ps1"; powershell -ExecutionPolicy Bypass -File "$env:TEMP\install-codex-provider.ps1"
    ```
  </Tab>
</Tabs>

Après l’installation, comparez les deux fichiers à la configuration manuelle ci-dessous et corrigez les différences avant de lancer Codex.

## Configuration manuelle

### Modifiez config.toml

Placez `model_provider`, `model` et `cli_auth_credentials_store` au niveau racine, avant toutes les tables TOML, et non dans un profil. L’ID du fournisseur doit correspondre au nom de sa table. Pour cette authentification par fichiers, retirez `env_key` et `experimental_bearer_token` du fournisseur sélectionné.

```toml
model_provider = "bettertoken"
model = "gpt-6-astra"
cli_auth_credentials_store = "file"

[model_providers.bettertoken]
name = "BetterToken"
base_url = "{{openai-base-url}}"
wire_api = "responses"
requires_openai_auth = true
request_max_retries = 4
stream_max_retries = 8
stream_idle_timeout_ms = 300000
supports_websockets = false
```

L’exemple utilise GPT-6 Astra (`gpt-6-astra`). Pour un autre modèle, copiez son ID complet depuis la <a href={"{{model-plaza-url}}"}>model plaza</a> et remplacez `model`. GPT, Kimi et GLM utilisent la même structure d’authentification par fichiers ; la configuration automatique est disponible pour GPT.

### Modifiez auth.json

`auth.json` se trouve dans le même répertoire que `config.toml`. Si `auth.json` n’existe pas, créez ce fichier dans ce répertoire. Utilisez le JSON suivant comme contenu du fichier, remplacez `YOUR_API_KEY` par votre BetterToken API Key, puis enregistrez le fichier.

```json
{
  "auth_mode": "apikey",
  "OPENAI_API_KEY": "YOUR_API_KEY"
}
```

## Vérifier la connexion

Enregistrez les deux fichiers. Lancez une nouvelle session Codex CLI directement dans le terminal :

```bash
codex
```

Quittez complètement Codex App, y compris les processus en arrière-plan. Rouvrez l’application et créez une nouvelle tâche locale. Envoyez une courte consigne dans chaque client utilisé. Une réponse normale vérifie la connexion de ce client.

## Changer de modèle

Modifiez `model` et, si nécessaire, `review_model`. Enregistrez le fichier, arrêtez le processus Codex en cours et relancez-le. Utilisez le Model ID de la boîte de dialogue Setup ou du model plaza.

## Erreurs fréquentes

En cas de `401`, `403` ou de demande de connexion, vérifiez le répertoire actif, la Key dans `auth.json`, `auth_mode = apikey`, `cli_auth_credentials_store = file` et `requires_openai_auth = true`. Pour une erreur de fournisseur ou de modèle, vérifiez l’ID du fournisseur, `{{openai-base-url}}`, `wire_api = responses` et la disponibilité du modèle. Si les changements ne s’appliquent pas, vérifiez les paramètres prioritaires et assurez-vous que CLI et App utilisent le même `CODEX_HOME`.

## Configuration avancée

{/* tool-provider-support:start */}
### Fournisseurs compatibles

| Fournisseur | État |
| --- | --- |
| Claude | Non compatible |
| GPT | Ligne de commande et configuration manuelle |
| Kimi | Configuration manuelle |
| GLM | Configuration manuelle |

<Note>Les états concernent la méthode de configuration de BetterToken décrite sur cette page.</Note>

<Accordion title="Signification des méthodes de configuration">
  - **Configuration en ligne de commande et manuelle** : utilisez une commande générée ou suivez toutes les étapes manuelles.
  - **Configuration manuelle** : saisissez l’API Key, la Base URL et le Model.
  - **Non compatible** : aucune méthode de connexion directe vérifiée n’est disponible pour le moment.
</Accordion>
{/* tool-provider-support:end */}

### Extension Codex

L’extension Codex pour VS Code partage le fichier `config.toml`, mais la conservation de la connexion officielle nécessite une autre configuration d’authentification. Consultez le [guide de configuration de l’extension Codex](/fr/ai-tools/codex-vscode).

### Questions associées

- [Qu’est-ce que Codex CLI ?](/fr/faq/codex/what-is-codex-cli)
- [Codex CLI, Codex App ou Codex Web](/fr/faq/codex/codex-cli-vs-app-vs-web)
- [Guide de configuration de config.toml pour Codex](/fr/faq/codex/config-toml)
- [Que signifient model_provider, base_url et wire_api ?](/fr/faq/codex/model-provider-base-url-wire-api)
- [Que signifient review_model et reasoning_effort ?](/fr/faq/codex/review-model-reasoning-effort)
- [Qu’est-ce que AGENTS.md et comment le rédiger ?](/fr/faq/codex/agents-md)
- [Que sont les modes sandbox et approval de Codex CLI ?](/fr/faq/codex/sandbox-approval)
- [Conserver la connexion officielle de Codex avec une API tierce](/fr/faq/codex/official-login-third-party-api)
- [API compatible avec OpenAI ou API compatible avec Anthropic](/fr/faq/concepts/openai-compatible-vs-anthropic-compatible)
- [Claude Code ou Codex CLI](/fr/faq/concepts/claude-code-vs-codex-cli)
- [Gérer le fournisseur Codex avec CC Switch](/fr/ai-tools/cc-switch#codex-cli)

### Référence officielle

- [Référence de configuration Codex](https://developers.openai.com/codex/config-reference)
- [Référence officielle d’authentification](https://developers.openai.com/codex/auth)

## Détails techniques

<Accordion title="Protocole Responses">
  Codex utilise OpenAI Responses. Définissez `base_url` sur `{{openai-base-url}}` et conservez `wire_api = "responses"`. Codex ajoute `/responses`.
</Accordion>
