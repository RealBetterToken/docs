---
title: "Image à image (édition d’image)"
description: "Générez ou éditez des images à partir d’images de référence avec GPT Image 2 via l’API OpenAI compatible de BetterToken. Avec configuration et exemples."
openapi: "api-reference/openapi.en.json POST /v1/images/edits"
---

`POST /v1/images/edits`

L’endpoint image à image utilise un corps de Request `multipart/form-data`. Téléversez une ou plusieurs images de référence et envoyez les champs de prompt et de paramètres sous forme de champs de formulaire.

<Note>
  Utilisez `{{openai-base-url}}` comme `Base URL`. Passez votre BetterToken API Key via `Authorization: Bearer YOUR_API_KEY`.
</Note>

<Tip>
  Vous pouvez renseigner les champs de formulaire et téléverser des images de référence dans le Playground à droite de la page, puis envoyer la Request directement à `{{images-edits-url}}`.
</Tip>

<Warning>
  N’utilisez pas de corps JSON ordinaire pour les Request image à image. Envoyez les champs texte et fichier avec `multipart/form-data`.
</Warning>

## Valeurs recommandées

Envoyez explicitement ces champs dans chaque Request :

```text
model=gpt-image-2
n=1
response_format=b64_json
output_format=png
```

Utilisez `image` pour une seule image de référence. Utilisez des champs `image[]` répétés pour plusieurs images de référence.

## Une image de référence

```bash
curl '{{images-edits-url}}' \
  -H 'Authorization: Bearer sk-***REDACTED***' \
  -F 'model=gpt-image-2' \
  -F 'prompt=Use this reference image to create a more polished square product hero image while keeping the main style consistent.' \
  -F 'n=1' \
  -F 'size=1024x1024' \
  -F 'response_format=b64_json' \
  -F 'output_format=png' \
  -F 'image=@reference.png'
```

## Plusieurs images de référence

```bash
curl '{{images-edits-url}}' \
  -H 'Authorization: Bearer sk-***REDACTED***' \
  -F 'model=gpt-image-2' \
  -F 'prompt=Combine these reference images into one consistent promotional poster.' \
  -F 'n=1' \
  -F 'size=1024x1024' \
  -F 'response_format=b64_json' \
  -F 'output_format=png' \
  -F 'image[]=@reference-1.png' \
  -F 'image[]=@reference-2.jpg' \
  -F 'image[]=@reference-3.png'
```

## Modifier une zone sélectionnée avec un masque

Ajoutez le champ de fichier facultatif `mask` pour orienter la modification vers une zone sélectionnée. Le point de terminaison BetterToken actuel lit un seul fichier dans `mask`. N’envoyez pas plusieurs champs `mask[]`.

- L’image d’origine et le masque doivent utiliser le même format et les mêmes dimensions en pixels. Chaque fichier doit faire moins de 50 MB. Pour une compatibilité maximale, utilisez un PNG avec canal Alpha pour les deux fichiers.
- Le masque doit contenir un canal Alpha. Les pixels entièrement transparents (`Alpha 0`) indiquent la zone que le modèle peut redessiner. Les pixels entièrement opaques (`Alpha 255`) indiquent au modèle de préserver cette zone.
- Une image RVB ordinaire en noir et blanc n’est pas un masque valide si elle ne contient pas réellement de canal Alpha.
- Si vous envoyez plusieurs champs `image[]`, le masque s’applique à la première image d’entrée.
- Le masque guide le modèle. Il ne garantit pas que les pixels situés en dehors de la zone transparente resteront parfaitement identiques.

```bash
curl '{{images-edits-url}}' \
  -H 'Authorization: Bearer sk-***REDACTED***' \
  -F 'model=gpt-image-2' \
  -F 'prompt=Replace only the masked area with a blue circle. Preserve everything else.' \
  -F 'n=1' \
  -F 'size=1024x1024' \
  -F 'response_format=b64_json' \
  -F 'output_format=png' \
  -F 'image=@original.png' \
  -F 'mask=@mask.png'
```

### Édition avec masque en Python

Ouvrez les deux fichiers dans une seule instruction `with` afin que Python les ferme après la requête :

```python
import requests

url = "{{images-edits-url}}"
headers = {"Authorization": "Bearer sk-***REDACTED***"}
data = {
    "model": "gpt-image-2",
    "prompt": "Replace only the masked area with a blue circle. Preserve everything else.",
    "n": "1",
    "size": "1024x1024",
    "response_format": "b64_json",
    "output_format": "png",
}

with open("original.png", "rb") as image_file, open("mask.png", "rb") as mask_file:
    files = {
        "image": ("original.png", image_file, "image/png"),
        "mask": ("mask.png", mask_file, "image/png"),
    }
    response = requests.post(url, headers=headers, data=data, files=files, timeout=300)

response.raise_for_status()
result = response.json()
```

### Vérifier les fichiers avant l’envoi

Cette vérification minimale avec Pillow contrôle les exigences de format, de dimensions et de canal Alpha. Pour un masque local classique, elle affiche aussi un avertissement si le canal Alpha ne contient pas à la fois des zones entièrement transparentes et opaques :

```python
from pathlib import Path
from PIL import Image

image_path = Path("original.png")
mask_path = Path("mask.png")

if image_path.stat().st_size >= 50 * 1024 * 1024 or mask_path.stat().st_size >= 50 * 1024 * 1024:
    raise ValueError("Each file must be smaller than 50 MB")

with Image.open(image_path) as image, Image.open(mask_path) as mask:
    if image.format != mask.format:
        raise ValueError("The image and mask formats must match")
    if image.size != mask.size:
        raise ValueError("The image and mask dimensions must match")
    if "A" not in mask.getbands():
        raise ValueError("The mask must contain an Alpha channel")
    alpha_min, alpha_max = mask.getchannel("A").getextrema()
    if alpha_min > 0 or alpha_max < 255:
        print("Warning: for a typical local mask, include fully transparent and fully opaque areas")
```

## Exemple Python

```python
import requests

url = "{{images-edits-url}}"
headers = {
    "Authorization": "Bearer sk-***REDACTED***",
}

data = {
    "model": "gpt-image-2",
    "prompt": "Use this reference image to create a more polished square product hero image while keeping the main style consistent.",
    "n": "1",
    "size": "1024x1024",
    "response_format": "b64_json",
    "output_format": "png",
}

with open("reference.png", "rb") as image_file:
    files = {
        "image": ("reference.png", image_file, "image/png"),
    }
    response = requests.post(url, headers=headers, data=data, files=files, timeout=300)

response.raise_for_status()
result = response.json()
b64_json = result["data"][0]["b64_json"]
```

## Tailles recommandées

| `size` | Ratio | Cas d’utilisation |
| --- | --- | --- |
| `auto` | Auto | Sélection automatique de la taille |
| `1024x1024` | `1:1` | Images carrées, avatars, couvertures, ressources |
| `1536x1024` | `3:2` | Affiches, bannières et scènes au format paysage |
| `1024x1536` | `2:3` | Couvertures et affiches mobiles au format portrait |
| `1536x1152` | `4:3` | Images paysage standard, images de produit, illustrations de contenu |
| `1152x1536` | `3:4` | Images portrait standard, couvertures mobiles, affiches verticales |
| `2048x2048` | `1:1` | Images carrées haute résolution |
| `2048x1152` | `16:9` | Images paysage haute résolution |
| `3840x2160` | `16:9` | Images paysage 4K |
| `2160x3840` | `9:16` | Images portrait 4K |

`size` représente le ratio attendu et le niveau de taille. Les pixels réellement renvoyés peuvent être mappés ou ajustés par le serveur. Utilisez les dimensions de l’image décodée plutôt que de recadrer de force la sortie à la valeur demandée.

## Enregistrer l’image

Une réponse réussie suit la structure de réponse d’image OpenAI compatible :

```json
{
  "created": 1710000000,
  "data": [
    {
      "b64_json": "iVBORw0KGgoAAAANSUhEUgAA...(truncated)"
    }
  ]
}
```

Lisez `data[0].b64_json` et enregistrez-le comme contenu d’image base64. Définissez toujours `output_format: "png"` afin de pouvoir enregistrer l’image décodée sous `.png`.

## Flux de réponse

Cet endpoint est synchrone. Après l’envoi de `POST /images/edits`, gardez la Request HTTP actuelle ouverte jusqu’à la réponse du serveur. Lorsque la génération réussit, le contenu de l’image est renvoyé dans `data[0].b64_json`.

L’endpoint ne renvoie pas de `task_id`, et il n’existe pas d’endpoint distinct de requête de statut ou de téléchargement de résultat.

## Documents associés

- [Texte à image](/fr/api-reference/images-generations)
- [GPT Image 2 est disponible](/fr/model-updates/gpt-image-2)
