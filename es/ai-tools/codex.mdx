---
title: "Proveedor personalizado de Codex CLI/App: config.toml, API Key y Base URL"
sidebarTitle: "Codex"
description: "Configura Codex CLI y Codex App con config.toml, auth.json, una BetterToken API Key y el Base URL de Responses."
icon: "code"
---

Codex CLI y Codex App comparten `config.toml` y `auth.json` para las tareas locales. Esta configuración usa una BetterToken API Key mediante OpenAI Responses.

## Ajustes principales

| Campo | Valor |
| --- | --- |
| API Key | `auth.json` → `OPENAI_API_KEY` |
| Base URL | `{{openai-base-url}}` |
| Model | `gpt-6-astra` |

## Requisitos previos

- Instala Node.js
- <a href={"{{register-url}}"}>Crea una BetterToken API Key</a>
- Copia un Model ID del <a href={"{{model-plaza-url}}"}>model plaza</a> o del cuadro de diálogo **Setup** de la Key

## Instalación

```bash
npm install -g @openai/codex
```

## Localiza los archivos y crea copias de seguridad

Si has definido un `CODEX_HOME` personalizado, edita allí `config.toml` y `auth.json`. En caso contrario, usa `~/.codex` en macOS / Linux o `%USERPROFILE%\.codex` en Windows. CLI y App deben usar el mismo directorio. WSL tiene su propio directorio de usuario.

Cierra Codex por completo antes de editar. Guarda copias de ambos archivos en un lugar privado. Crea los archivos que falten. Sustituye los ajustes existentes sin duplicar claves ni tablas; conserva los demás ajustes.

<Warning>
  Esta configuración cambia el inicio de sesión con ChatGPT por la autenticación con API Key. No conserva el acceso a la cuenta oficial. Protege `auth.json` y sus copias; no los compartas ni los incluyas en el repositorio.
</Warning>

## Configuración por línea de comandos

El script de configuración automática de BetterToken escribe la configuración del proveedor para Codex. Requiere Node.js y solicita la API Key o el Model ID si no se proporcionan como argumentos.

<Tabs>
  <Tab title="macOS / Linux">
    ```bash
    curl -fsSL "{{install-codex-provider-sh-url}}" | bash
    ```
  </Tab>
  <Tab title="Windows">
    ```powershell
    iwr "{{install-codex-provider-ps1-url}}" -OutFile "$env:TEMP\install-codex-provider.ps1"; powershell -ExecutionPolicy Bypass -File "$env:TEMP\install-codex-provider.ps1"
    ```
  </Tab>
</Tabs>

Tras la instalación, compara ambos archivos con la configuración manual siguiente y corrige las diferencias antes de iniciar Codex.

## Configuración manual

### Edita config.toml

Coloca `model_provider`, `model` y `cli_auth_credentials_store` en el nivel superior, antes de todas las tablas TOML, no dentro de un perfil. El ID del proveedor debe coincidir con el nombre de su tabla. Para esta autenticación por archivos, elimina `env_key` y `experimental_bearer_token` del proveedor seleccionado.

```toml
model_provider = "bettertoken"
model = "gpt-6-astra"
cli_auth_credentials_store = "file"

[model_providers.bettertoken]
name = "BetterToken"
base_url = "{{openai-base-url}}"
wire_api = "responses"
requires_openai_auth = true
request_max_retries = 4
stream_max_retries = 8
stream_idle_timeout_ms = 300000
supports_websockets = false
```

El ejemplo usa GPT-6 Astra (`gpt-6-astra`). Para otro modelo, copia su ID completo de la <a href={"{{model-plaza-url}}"}>model plaza</a> y sustituye `model`. GPT, Kimi y GLM usan la misma estructura de autenticación por archivos; la configuración automática está disponible para GPT.

### Edita auth.json

`auth.json` está en el mismo directorio que `config.toml`. Si no existe `auth.json`, créalo en ese directorio. Usa el siguiente JSON como contenido del archivo, reemplaza `YOUR_API_KEY` por tu BetterToken API Key y guarda el archivo.

```json
{
  "auth_mode": "apikey",
  "OPENAI_API_KEY": "YOUR_API_KEY"
}
```

## Verificar la conexión

Guarda ambos archivos. Inicia una nueva sesión de Codex CLI directamente desde el terminal:

```bash
codex
```

Cierra Codex App por completo, incluidos los procesos en segundo plano. Ábrelo de nuevo y crea una tarea local nueva. Envía una indicación breve en cada cliente que uses. Una respuesta normal verifica la conexión de ese cliente.

## Cambiar de modelo

Cambia `model` y, de forma opcional, `review_model`. Guarda el archivo, detén el proceso actual de Codex y vuelve a iniciarlo. Usa el Model ID del cuadro de diálogo Setup o del model plaza.

## Errores frecuentes

Ante `401`, `403` o una solicitud de inicio de sesión, revisa el directorio activo, la Key de `auth.json`, `auth_mode = apikey`, `cli_auth_credentials_store = file` y `requires_openai_auth = true`. Ante errores del proveedor o modelo, comprueba el ID del proveedor, `{{openai-base-url}}`, `wire_api = responses` y la disponibilidad del modelo. Si los cambios no se aplican, revisa las opciones que sobrescriben la configuración y confirma que CLI y App usan el mismo `CODEX_HOME`.

## Configuración avanzada

{/* tool-provider-support:start */}
### Proveedores compatibles

| Proveedor | Estado |
| --- | --- |
| Claude | No compatible |
| GPT | Línea de comandos y configuración manual |
| Kimi | Configuración manual |
| GLM | Configuración manual |

<Note>Los estados se aplican al método de configuración de BetterToken descrito en esta página.</Note>

<Accordion title="Qué significa cada método de configuración">
  - **Configuración por línea de comandos y manual**: usa un comando generado o sigue todos los pasos manuales.
  - **Configuración manual**: introduce la API Key, la Base URL y el Model.
  - **No compatible**: todavía no hay un método de conexión directa verificado.
</Accordion>
{/* tool-provider-support:end */}

### Extensión de Codex

La extensión de Codex para VS Code comparte `config.toml`, pero conservar el inicio de sesión oficial requiere una configuración de autenticación diferente. Consulta la [guía de configuración de la extensión de Codex](/es/ai-tools/codex-vscode).

### Preguntas relacionadas

- [Qué es Codex CLI](/es/faq/codex/what-is-codex-cli)
- [Codex CLI frente a Codex App y Codex Web](/es/faq/codex/codex-cli-vs-app-vs-web)
- [Guía de configuración de config.toml en Codex](/es/faq/codex/config-toml)
- [Qué son model_provider, base_url y wire_api](/es/faq/codex/model-provider-base-url-wire-api)
- [Qué significan review_model y reasoning_effort](/es/faq/codex/review-model-reasoning-effort)
- [Qué es AGENTS.md y cómo debe escribirse](/es/faq/codex/agents-md)
- [Qué son los modos sandbox y approval de Codex CLI](/es/faq/codex/sandbox-approval)
- [Cómo conservar el inicio de sesión oficial de Codex con una API de terceros](/es/faq/codex/official-login-third-party-api)
- [API compatible con OpenAI frente a API compatible con Anthropic](/es/faq/concepts/openai-compatible-vs-anthropic-compatible)
- [Claude Code frente a Codex CLI](/es/faq/concepts/claude-code-vs-codex-cli)
- [Administrar el proveedor de Codex con CC Switch](/es/ai-tools/cc-switch#codex-cli)

### Referencia oficial

- [Referencia de configuración de Codex](https://developers.openai.com/codex/config-reference)
- [Referencia oficial de autenticación](https://developers.openai.com/codex/auth)

## Detalles técnicos

<Accordion title="Protocolo Responses">
  Codex usa OpenAI Responses. Configura `base_url` como `{{openai-base-url}}` y mantén `wire_api = "responses"`. Codex añade `/responses`.
</Accordion>
