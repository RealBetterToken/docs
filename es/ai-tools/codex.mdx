---
title: "Proveedor personalizado de Codex CLI/App: config.toml, API Key y Base URL"
sidebarTitle: "Codex"
description: "Configura un proveedor personalizado de Codex CLI/App con config.toml, BETTERTOKEN_API_KEY, una Base URL de Responses y un Model ID actual."
icon: "code"
---

Codex CLI y Codex App se conectan a BetterToken mediante OpenAI Responses. La configuración automática usa el inicio de sesión con API Key de Codex. La configuración manual mantiene la credencial en `BETTERTOKEN_API_KEY`.

## Ajustes principales

| Campo | Valor |
| --- | --- |
| API Key | Automática: inicio de sesión con API Key de Codex; manual: `BETTERTOKEN_API_KEY` |
| Base URL | `{{openai-base-url}}` |
| Model | `YOUR_MODEL_ID` |

## Requisitos previos

- Instala Node.js
- <a href={"{{register-url}}"}>Crea una BetterToken API Key</a>
- Copia un Model ID del <a href={"{{model-plaza-url}}"}>model plaza</a> o del cuadro de diálogo **Setup** de la Key

## Instalación

```bash
npm install -g @openai/codex
```

## Configuración por línea de comandos

El script de configuración automática de BetterToken escribe la configuración del proveedor para Codex y guarda la BetterToken API Key en `~/.codex/auth.json`. Establece `requires_openai_auth = true` para que Codex use el inicio de sesión con API Key. Requiere Node.js y solicita la API Key o el Model ID si no se proporcionan como argumentos.

<Warning>
  La configuración automática cambia el inicio de sesión oficial de ChatGPT por el inicio de sesión con API Key de Codex. Para conservar el inicio de sesión oficial, omite el script y usa la configuración manual del proveedor que aparece más abajo.
</Warning>

<Tabs>
  <Tab title="macOS / Linux">
    ```bash
    curl -fsSL "{{install-codex-provider-sh-url}}" | bash
    ```
  </Tab>
  <Tab title="Windows">
    ```powershell
    iwr "{{install-codex-provider-ps1-url}}" -OutFile "$env:TEMP\install-codex-provider.ps1"; powershell -ExecutionPolicy Bypass -File "$env:TEMP\install-codex-provider.ps1"
    ```
  </Tab>
</Tabs>

Cuando termine, abre `~/.codex/config.toml` (o `%USERPROFILE%\.codex\config.toml` en Windows). Confirma que `model`, `review_model`, el proveedor BetterToken y `requires_openai_auth = true` se hayan escrito correctamente. Después, sigue los pasos de reinicio de **Verificar la conexión**.

## Configuración manual

GPT, Kimi y GLM usan la misma estructura de `config.toml`. Sustituye únicamente `YOUR_MODEL_ID` por un Model ID actual del proveedor seleccionado. La configuración por línea de comandos con un solo comando está disponible actualmente solo para GPT.

### Configurar la API Key

<Tabs>
  <Tab title="macOS / Linux">
    ```bash
    export BETTERTOKEN_API_KEY="YOUR_API_KEY"
    ```

    Añade esta línea a `~/.zshrc` o `~/.bashrc` para conservarla.
  </Tab>
  <Tab title="Windows PowerShell">
    ```powershell
    [Environment]::SetEnvironmentVariable("BETTERTOKEN_API_KEY", "YOUR_API_KEY", "User")
    $env:BETTERTOKEN_API_KEY = "YOUR_API_KEY"
    ```

    El primer comando conserva la variable para procesos futuros. El segundo la hace disponible en la sesión actual de PowerShell antes de iniciar Codex CLI.
  </Tab>
  <Tab title="Codex App">
    ```dotenv
    BETTERTOKEN_API_KEY="YOUR_API_KEY"
    ```

    Guarda esta línea en `~/.codex/.env` o en `%USERPROFILE%\.codex\.env` en Windows. Si usas un `CODEX_HOME` personalizado, guarda `.env` en ese directorio. Cierra Codex App por completo y vuelve a abrirla después de modificar el archivo.
  </Tab>
</Tabs>

<Warning>
  Con este método manual, no guardes la BetterToken API Key en `~/.codex/auth.json`. Conserva allí el inicio de sesión oficial y deja que el proveedor personalizado lea `BETTERTOKEN_API_KEY` mediante `env_key`. El método automático anterior es un modo de inicio de sesión con API Key independiente.
</Warning>

### Configurar config.toml

Edita `~/.codex/config.toml`:

```toml
model_provider = "custom"
model = "YOUR_MODEL_ID"
review_model = "YOUR_MODEL_ID"
model_reasoning_effort = "high"

[model_providers.custom]
name = "BetterToken"
base_url = "{{openai-base-url}}"
env_key = "BETTERTOKEN_API_KEY"
wire_api = "responses"
requires_openai_auth = false
request_max_retries = 4
stream_max_retries = 8
stream_idle_timeout_ms = 300000
supports_websockets = false
```

`model_provider = "custom"` debe coincidir con la tabla `[model_providers.custom]`. Sustituye ambos valores `YOUR_MODEL_ID` por un Model ID completo y actual.

## Verificar la conexión

<Tabs>
  <Tab title="Codex CLI">
    Detén el proceso actual de Codex. Abre una terminal nueva y ejecuta:

    ```bash
    codex
    ```

    Envía una tarea corta. Una respuesta en streaming sin errores de inicio de sesión, `401` o modelo confirma la configuración.
  </Tab>
  <Tab title="Codex App">
    Cierra Codex App por completo, vuelve a abrirla y crea una tarea nueva. Envía una tarea corta. Una respuesta en streaming sin errores de inicio de sesión, `401` o modelo confirma la configuración.
  </Tab>
</Tabs>

## Cambiar de modelo

Cambia `model` y, de forma opcional, `review_model`. Guarda el archivo, detén el proceso actual de Codex y vuelve a iniciarlo. Usa el Model ID del cuadro de diálogo Setup o del model plaza.

## Errores frecuentes

| Error | Solución |
| --- | --- |
| `401` o solicitud de inicio de sesión de OpenAI | Automática: confirma que el script haya terminado y que `requires_openai_auth = true`. Manual: comprueba `BETTERTOKEN_API_KEY`, `env_key` y `requires_openai_auth = false`. |
| No se encuentra el proveedor | Haz que `model_provider = "custom"` coincida con `[model_providers.custom]`. |
| Error de modo API | Mantén `wire_api = "responses"`; no cambies a Chat Completions. |
| No se encuentra el modelo | Sustituye `YOUR_MODEL_ID` por un ID completo y actual del cuadro Setup o del model plaza. |
| La configuración no se aplica | Para CLI, abre una terminal nueva. Para Codex App, ciérrala por completo, vuelve a abrirla y crea una tarea nueva. En modo manual, verifica también que el proceso activo pueda leer `BETTERTOKEN_API_KEY`. |

## Configuración avanzada

{/* tool-provider-support:start */}
### Proveedores compatibles

| Proveedor | Estado |
| --- | --- |
| Claude | No compatible |
| GPT | Línea de comandos y configuración manual |
| Kimi | Configuración manual |
| GLM | Configuración manual |

<Note>Los estados se aplican al método de configuración de BetterToken descrito en esta página.</Note>

<Accordion title="Qué significa cada método de configuración">
  - **Configuración por línea de comandos y manual**: usa un comando generado o sigue todos los pasos manuales.
  - **Configuración manual**: introduce la API Key, la Base URL y el Model.
  - **No compatible**: todavía no hay un método de conexión directa verificado.
</Accordion>
{/* tool-provider-support:end */}

### Extensión de Codex

La extensión de Codex para VS Code comparte `config.toml`, pero conservar el inicio de sesión oficial requiere una configuración de autenticación diferente. Consulta la [guía de configuración de la extensión de Codex](/es/ai-tools/codex-vscode).

<Warning>
  La configuración de CLI/App y la de la extensión con inicio de sesión oficial son alternativas. No pegues dos tablas `[model_providers.custom]` en el mismo archivo TOML.
</Warning>

### Preguntas relacionadas

- [Qué es Codex CLI](/es/faq/codex/what-is-codex-cli)
- [Codex CLI frente a Codex App y Codex Web](/es/faq/codex/codex-cli-vs-app-vs-web)
- [Guía de configuración de config.toml en Codex](/es/faq/codex/config-toml)
- [Qué son model_provider, base_url y wire_api](/es/faq/codex/model-provider-base-url-wire-api)
- [Qué significan review_model y reasoning_effort](/es/faq/codex/review-model-reasoning-effort)
- [Qué es AGENTS.md y cómo debe escribirse](/es/faq/codex/agents-md)
- [Qué son los modos sandbox y approval de Codex CLI](/es/faq/codex/sandbox-approval)
- [Cómo conservar el inicio de sesión oficial de Codex con una API de terceros](/es/faq/codex/official-login-third-party-api)
- [API compatible con OpenAI frente a API compatible con Anthropic](/es/faq/concepts/openai-compatible-vs-anthropic-compatible)
- [Claude Code frente a Codex CLI](/es/faq/concepts/claude-code-vs-codex-cli)
- [Administrar el proveedor de Codex con CC Switch](/es/ai-tools/cc-switch#codex-cli)

### Referencia oficial

- [Referencia de configuración de Codex](https://developers.openai.com/codex/config-reference)

## Detalles técnicos

<Accordion title="Protocolo Responses">
  Codex usa OpenAI Responses. Configura `base_url` como `{{openai-base-url}}` y mantén `wire_api = "responses"`. Codex añade `/responses`.
</Accordion>
