---
title: "Configurar Hermes Agent: instalación, API Key, Base URL y modelos personalizados"
sidebarTitle: "Hermes Agent"
description: "Instala Hermes Agent, configura la Base URL y la API Key de BetterToken, elige un modelo de un proveedor compatible y corrige los errores habituales."
icon: "user-astronaut"
---

Para conectar Hermes Agent con BetterToken, prepara una API Key, introduce la Base URL indicada a continuación y elige un ID de modelo vigente en la model plaza.

## Ajustes principales

| Campo | Valor |
| --- | --- |
| API Key | BetterToken API Key |
| Base URL | `{{openai-base-url}}` |
| Modelo | Un ID de modelo vigente de la <a href={"{{model-plaza-url}}"}>model plaza</a> |

## Requisitos previos

- La versión actual de la herramienta está instalada
- Una BetterToken API Key: <a href={"{{register-url}}"}>regístrate y obtén una</a>
- Un ID de modelo de la <a href={"{{model-plaza-url}}"}>model plaza</a>

### Requisitos adicionales

- El ID de modelo seleccionado procede del **proveedor compatible**
- macOS, Linux, Windows 10/11 o WSL2
- Un modelo con una ventana de contexto mínima de 64K para usar herramientas

## Instalación

### Instala Hermes Agent

<Tabs>
  <Tab title="Desktop (macOS / Windows)">
    Hermes recomienda el instalador Desktop para macOS y Windows. [Descarga Hermes Desktop](https://hermes-agent.nousresearch.com/) y ejecuta el instalador de tu sistema.
  </Tab>

  <Tab title="Linux / macOS / WSL2">
    ```bash
    curl -fsSL https://hermes-agent.nousresearch.com/install.sh | bash
    ```
  </Tab>

  <Tab title="Windows PowerShell">
    Hermes Agent es compatible de forma nativa con Windows 10 y 11. Ejecuta esto en PowerShell o Windows Terminal:

    ```powershell
    iex (irm https://hermes-agent.nousresearch.com/install.ps1)
    ```

    No necesitas permisos de administrador. Abre una nueva ventana de PowerShell después de instalar.
  </Tab>
</Tabs>

```bash
hermes --version
hermes doctor
```

<Note>WSL2 sigue siendo compatible, pero es opcional. Úsalo si necesitas un entorno Linux o el terminal integrado del Web Dashboard.</Note>

### Referencia rápida de funciones

| Función | Uso |
| --- | --- |
| Terminal UI | Conversar directamente con el agente desde la terminal |
| Skills | Guardar workflows reutilizables para tareas repetidas |
| MCP | Conectar capacidades de herramientas locales o remotas |
| Tareas programadas | Ejecutar trabajos temporizados y workflows recurrentes |
| Subagentes | Dividir trabajos complejos entre agentes especializados |
| Gateway de mensajes | Conectar el agente con más puntos de entrada de mensajes |

## Configuración manual

Usa como método principal la configuración interactiva de modelos de Hermes Agent. Edita directamente los archivos de configuración solo cuando necesites una configuración manual.

| Campo | Valor |
| --- | --- |
| API Key | BetterToken API Key |
| Base URL | `{{openai-base-url}}` |
| Modelo | Un ID de modelo vigente de la <a href={"{{model-plaza-url}}"}>model plaza</a> |

### Pasos de configuración

<Tabs>
  <Tab title="Configuración interactiva">
    <Steps>
      <Step title="Ejecuta el selector de modelos">
        Si hay un chat de Hermes abierto, sal primero de la sesión. Después, ejecuta:

        ```bash
        hermes model
        ```
      </Step>

      <Step title="Elige Custom endpoint">
        Selecciona **Custom endpoint (self-hosted / VLLM / etc.)** en la lista de proveedores.
      </Step>

      <Step title="Completa la configuración de BetterToken">
        Introduce:

        - **Base URL**: `{{openai-base-url}}`
        - **API Key**: tu BetterToken API Key
        - **Model**: un **ID de modelo de un proveedor compatible** copiado desde la <a href={"{{model-plaza-url}}"}>model plaza</a>

        Si Hermes pregunta por el modo de API, elige **Chat Completions**. Para usar las herramientas del agente, selecciona un modelo con al menos 64K de contexto.
      </Step>

      <Step title="Inicia Hermes Agent">
        ```bash
        hermes --tui
        ```

        Ejecuta `hermes` si prefieres la interfaz clásica de terminal.
      </Step>
    </Steps>
  </Tab>

  <Tab title="Archivos de configuración">
    Añade el proveedor custom en `~/.hermes/config.yaml`:

    ```yaml
    model:
      default: YOUR_MODEL_ID
      provider: custom
      base_url: "{{openai-base-url}}"
    ```

    Guarda la BetterToken API Key por separado en `~/.hermes/.env`:

    ```dotenv
    OPENAI_API_KEY=YOUR_API_KEY
    ```

    Sustituye `YOUR_MODEL_ID` por un ID de modelo de un proveedor compatible de la <a href={"{{model-plaza-url}}"}>model plaza</a> y `YOUR_API_KEY` por tu BetterToken API Key. En Windows nativo, estos archivos están en `%LOCALAPPDATA%\hermes\`.
  </Tab>
</Tabs>

### Migra desde OpenClaw

Si ya usas OpenClaw, comprueba primero si tu versión actual de Hermes Agent incluye el comando de migración. Ejecuta un dry run antes de cambiar nada.

```bash
hermes claw migrate --dry-run
```

Después de confirmar la salida, ejecuta la migración:

```bash
hermes claw migrate
```

Después de migrar, revisa la **Base URL**, la API Key y el ID del modelo de Hermes Agent. Deben coincidir con `{{openai-base-url}}`, tu BetterToken API Key y un ID de modelo de un proveedor compatible.

## Verifica la conexión

Inicia Hermes Agent y envía este prompt de prueba: `Reply with HERMES_OK only. Do not use tools.` Si la respuesta es exactamente `HERMES_OK`, la conexión funciona.

## Cambia de modelo

Fuera de una sesión, ejecuta `hermes model`. En un chat activo, usa `/model` para cambiar entre proveedores o modelos ya configurados. Elige el ID de modelo exacto de la <a href={"{{model-plaza-url}}"}>model plaza</a>.

## Errores habituales

| Error | Qué comprobar |
| --- | --- |
| `401` o `Unauthorized` | Comprueba la BetterToken API Key y elimina los espacios iniciales o finales. |
| `404` | Comprueba la Base URL y no añadas una ruta específica de endpoint. |
| `model not found` | Copia un ID de modelo vigente desde la model plaza. |
| Los cambios no se aplican | Guarda la configuración y reinicia la herramienta por completo. |


### Solución de problemas

- Si las solicitudes fallan o no responden, confirma que **Base URL** sea exactamente `{{openai-base-url}}`. No añadas `/chat/completions` en este campo.
- Si Hermes indica que el modelo no existe, confirma que **Model** sea un ID de modelo de un proveedor compatible de la <a href={"{{model-plaza-url}}"}>model plaza</a>.
- Si falla la autenticación, busca espacios copiados y confirma que el ID seleccionado proceda del **proveedor compatible**.
- Si Hermes sigue usando el modelo antiguo después de un cambio, cierra la sesión actual de Hermes Agent y vuelve a ejecutar `hermes`.
- BetterToken normalmente no requiere un `User-Agent` adicional ni cabeceras personalizadas. Añade cabeceras solo cuando tu gateway personalizado, el enrutamiento por tenant, la observabilidad o la capa de caché las exijan explícitamente.

<Note>
  No incluyas API Keys reales en los commits del repositorio. Para configuraciones compartidas por un equipo, usa preferentemente variables de entorno o configuración local.
</Note>

### Preguntas relacionadas

- [MCP frente a API Key y Base URL](/es/faq/concepts/mcp-vs-api-key-base-url)
- [¿Qué son model_provider, base_url y wire_api?](/es/faq/codex/model-provider-base-url-wire-api)
- [Cómo configurar una API compatible con OpenAI en Cline](/es/faq/cline/openai-compatible-api)
- [Guía para configurar BetterToken en OpenClaw](/es/ai-tools/openclaw)

## Configuración avanzada

{/* tool-provider-support:start */}
### Proveedores compatibles

| Proveedor | Estado |
| --- | --- |
| Claude | No compatible |
| GPT | Configuración manual |
| Kimi | Configuración manual |
| GLM | Configuración manual |

<Note>Los estados se aplican al método de configuración de BetterToken descrito en esta página.</Note>

<Accordion title="Qué significa cada método de configuración">
  - **Configuración manual**: introduce la API Key, la Base URL y el Model.
  - **No compatible**: todavía no hay un método de conexión directa verificado.
</Accordion>
{/* tool-provider-support:end */}

## Detalles técnicos

<Accordion title="Protocolo, endpoint y campos internos del proveedor">
  Esta configuración usa `{{openai-base-url}}`. La herramienta añade la ruta del endpoint compatible con OpenAI. No añadas `/chat/completions` ni `/responses` salvo que un campo concreto lo exija explícitamente.
</Accordion>
