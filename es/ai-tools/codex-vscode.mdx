---
title: "Extensión Codex para VS Code: Base URL personalizada, API Key e inicio de sesión oficial"
sidebarTitle: "Extensión Codex para VS Code"
description: "Configura el config.toml compartido de la extensión Codex para VS Code, usa BetterToken Responses y conserva el inicio de sesión oficial, los plugins y Remote Control."
icon: "/icons/codex-vscode.svg"
---

La extensión Codex para VS Code comparte `~/.codex/config.toml` con Codex CLI/App. Usa esta página si necesitas conservar el inicio de sesión oficial, los plugins o Remote Control.

## Ajustes principales

| Campo | Valor |
| --- | --- |
| API Key | `experimental_bearer_token = "YOUR_API_KEY"` |
| Base URL | `{{openai-base-url}}` |
| Model | `gpt-6-astra` |

## Requisitos previos

- Instala la versión más reciente de la extensión Codex para VS Code
- Completa el inicio de sesión oficial en la extensión o Codex App
- <a href={"{{register-url}}"}>Crea una BetterToken API Key</a>
- Copia un ID de modelo desde la <a href={"{{model-plaza-url}}"}>model plaza</a> o desde el cuadro **Setup** de la Key

## Instalación

Instala la extensión desde VS Code Marketplace y reinicia VS Code.

## Configuración manual

Si has definido un `CODEX_HOME` personalizado, edita `config.toml` en ese directorio. En caso contrario, usa `~/.codex/config.toml` en macOS / Linux o `%USERPROFILE%\.codex\config.toml` en Windows.

Cierra Codex por completo y guarda copias de los archivos `config.toml` y `auth.json` existentes. Actualiza la configuración del proveedor existente sin añadir una tabla de proveedor duplicada.

### Configura la extensión

Edita `~/.codex/config.toml`:

```toml
model_provider = "bettertoken"
model = "gpt-6-astra"

[model_providers.bettertoken]
name = "BetterToken"
base_url = "{{openai-base-url}}"
wire_api = "responses"
requires_openai_auth = true
experimental_bearer_token = "YOUR_API_KEY"
```

Guarda el archivo, ejecuta **Developer: Reload Window** en VS Code y vuelve a abrir el panel de Codex.

## Verifica la conexión

Crea una sesión nueva en la extensión y envía una tarea breve. Si `gpt-6-astra` responde y los plugins y Remote Control siguen disponibles, la configuración funciona.

## Cambia de modelo

Cambia `model` y, si lo necesitas, `review_model`; después, ejecuta **Developer: Reload Window**. Usa un ID de modelo del cuadro Setup o de la model plaza.

## Errores habituales

| Error | Solución |
| --- | --- |
| La extensión vuelve a pedir el inicio de sesión | Restaura el inicio de sesión oficial y mantén `requires_openai_auth = true`. |
| `401` o `403` | Vuelve a copiar `experimental_bearer_token`; no sobrescribas `auth.json`. |
| Tabla duplicada | Elimina la otra tabla `[model_providers.bettertoken]` y conserva un solo modo de autenticación. |
| Modelo no encontrado | Sustituye `gpt-6-astra` por un ID de modelo completo y vigente. |
| La configuración no se aplica | Guarda el archivo y ejecuta **Developer: Reload Window**. |

## Configuración avanzada

{/* tool-provider-support:start */}
### Proveedores compatibles

| Proveedor | Estado |
| --- | --- |
| Claude | No compatible |
| GPT | Configuración manual |
| Kimi | Configuración manual |
| GLM | Configuración manual |

<Note>Los estados se aplican al método de configuración de BetterToken descrito en esta página.</Note>

<Accordion title="Qué significa cada método de configuración">
  - **Configuración manual**: introduce la API Key, la Base URL y el Model.
  - **No compatible**: todavía no hay un método de conexión directa verificado.
</Accordion>
{/* tool-provider-support:end */}

### Guías relacionadas

- [Proveedor personalizado para Codex CLI/App](/es/ai-tools/codex)
- [Conservar el inicio de sesión oficial de Codex con una API de terceros](/es/faq/codex/official-login-third-party-api)
- [Guía de configuración de config.toml para Codex](/es/faq/codex/config-toml)
- [¿Qué son model_provider, base_url y wire_api?](/es/faq/codex/model-provider-base-url-wire-api)

### Referencias oficiales

- [Extensión Codex para IDE](https://developers.openai.com/codex/ide)
- [Configuración básica de Codex](https://developers.openai.com/codex/config-basic)
- [Referencia de configuración de Codex](https://developers.openai.com/codex/config-reference)

## Detalles técnicos

<Accordion title="Protocolo Responses">
  La extensión envía las solicitudes del modelo a `{{openai-base-url}}` mediante OpenAI Responses.
</Accordion>
