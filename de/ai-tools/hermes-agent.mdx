---
title: "Hermes Agent einrichten: Installation, API Key, Base URL und eigene Modelle"
sidebarTitle: "Hermes Agent"
description: "Installiere Hermes Agent, konfiguriere die BetterToken Base URL und API Key, wähle ein kompatibles Provider-Modell und behebe häufige Fehler."
icon: "user-astronaut"
---

Um Hermes Agent mit BetterToken zu verbinden, bereite einen API Key vor, trage die unten angezeigte Base URL ein und wähle eine aktuelle Model ID aus der model plaza.

## Wichtige Einstellungen

| Feld | Wert |
| --- | --- |
| API Key | BetterToken API Key |
| Base URL | `{{openai-base-url}}` |
| Model | Eine aktuelle Model ID aus der <a href={"{{model-plaza-url}}"}>model plaza</a> |

## Voraussetzungen

- Die aktuelle Version des Tools ist installiert
- Ein BetterToken API Key: <a href={"{{register-url}}"}>Registriere dich und erhalte einen</a>
- Eine Model ID aus der <a href={"{{model-plaza-url}}"}>model plaza</a>

### Zusätzliche Anforderungen

- Die ausgewählte Model ID stammt vom **unterstützten Provider**
- macOS, Linux, Windows 10/11 oder WSL2
- Ein Modell mit mindestens 64K Kontextfenster für die Tool-Nutzung

## Installation

### Hermes Agent installieren

<Tabs>
  <Tab title="Desktop (macOS / Windows)">
    Hermes empfiehlt den Desktop-Installer für macOS und Windows. [Hermes Desktop herunterladen](https://hermes-agent.nousresearch.com/) und den Installer für das eigene Betriebssystem ausführen.
  </Tab>

  <Tab title="Linux / macOS / WSL2">
    ```bash
    curl -fsSL https://hermes-agent.nousresearch.com/install.sh | bash
    ```
  </Tab>

  <Tab title="Windows PowerShell">
    Hermes Agent unterstützt Windows 10 und 11 nativ. Führe in PowerShell oder Windows Terminal Folgendes aus:

    ```powershell
    iex (irm https://hermes-agent.nousresearch.com/install.ps1)
    ```

    Administratorrechte sind nicht erforderlich. Öffne nach der Installation ein neues PowerShell-Fenster.
  </Tab>
</Tabs>

```bash
hermes --version
hermes doctor
```

<Note>WSL2 bleibt unterstützt, ist aber optional. Nutze es für eine Linux-Umgebung oder das eingebettete Terminal im Web Dashboard.</Note>

### Schnelle Funktionsübersicht

| Funktion | Zweck |
| --- | --- |
| Terminal UI | Direkt im Terminal mit dem Agent chatten |
| Skills | Wiederverwendbare Workflows für wiederkehrende Aufgaben speichern |
| MCP | Lokale oder entfernte Tool-Funktionen verbinden |
| Geplante Aufgaben | Zeitgesteuerte Jobs und wiederkehrende Workflows ausführen |
| Subagents | Komplexe Arbeit auf fokussierte Agents aufteilen |
| Message gateway | Den Agent mit weiteren Nachrichteneinstiegspunkten verbinden |

## Manuelle Einrichtung

Verwende die interaktive Hermes-Agent-Modell-Einrichtung als primären Weg. Bearbeite die Konfigurationsdateien nur dann direkt, wenn du eine manuelle Einrichtung benötigst.

| Feld | Wert |
| --- | --- |
| API Key | BetterToken API Key |
| Base URL | `{{openai-base-url}}` |
| Model | Eine aktuelle Model ID aus der <a href={"{{model-plaza-url}}"}>model plaza</a> |

### Einrichtung

<Tabs>
  <Tab title="Interactive setup">
    <Steps>
      <Step title="Modellauswahl ausführen">
        Wenn ein Hermes-Chat geöffnet ist, beende zuerst die Sitzung. Führe dann Folgendes aus:

        ```bash
        hermes model
        ```
      </Step>

      <Step title="Custom endpoint auswählen">
        Wähle **Custom endpoint (self-hosted / VLLM / etc.)** aus der Provider-Liste.
      </Step>

      <Step title="BetterToken-Konfiguration ausfüllen">
        Gib Folgendes ein:

        - **Base URL**: `{{openai-base-url}}`
        - **API Key**: dein BetterToken API Key
        - **Model**: eine **Model ID eines unterstützten Providers** aus der <a href={"{{model-plaza-url}}"}>model plaza</a>

        Wenn Hermes nach dem API-Modus fragt, wähle **Chat Completions**. Verwende für Agent-Tools ein Modell mit mindestens 64K Kontext.
      </Step>

      <Step title="Hermes Agent starten">
        ```bash
        hermes --tui
        ```

        Führe `hermes` aus, wenn du die klassische Terminal-Oberfläche bevorzugst.
      </Step>
    </Steps>
  </Tab>

  <Tab title="Konfigurationsdateien">
    Füge den Custom Provider in `~/.hermes/config.yaml` hinzu:

    ```yaml
    model:
      default: YOUR_MODEL_ID
      provider: custom
      base_url: "{{openai-base-url}}"
    ```

    Speichere den BetterToken API Key separat in `~/.hermes/.env`:

    ```dotenv
    OPENAI_API_KEY=YOUR_API_KEY
    ```

    Ersetze `YOUR_MODEL_ID` durch eine Model ID eines unterstützten Providers aus der <a href={"{{model-plaza-url}}"}>model plaza</a> und `YOUR_API_KEY` durch deinen BetterToken API Key. Unter nativem Windows liegen diese Dateien in `%LOCALAPPDATA%\hermes\`.
  </Tab>
</Tabs>

### Von OpenClaw migrieren

Wenn du bereits OpenClaw verwendest, prüfe zuerst, ob deine aktuelle Hermes-Agent-Version den Migrationsbefehl bereitstellt. Führe vor jeder Änderung einen Testlauf durch.

```bash
hermes claw migrate --dry-run
```

Führe nach Bestätigung der Ausgabe die Migration aus:

```bash
hermes claw migrate
```

Prüfe nach der Migration weiterhin die Hermes-Agent-**Base URL**, API Key und Model ID. Sie sollten `{{openai-base-url}}`, deinem BetterToken API Key und einer Model ID eines unterstützten Providers entsprechen.

## Verbindung überprüfen

Starte Hermes Agent und sende diese kurze Testanweisung: `Reply with HERMES_OK only. Do not use tools.` Wenn die Antwort genau `HERMES_OK` lautet, funktioniert die Verbindung.

## Modelle wechseln

Führe außerhalb einer Sitzung `hermes model` aus. Verwende in einem aktiven Chat `/model`, um zwischen bereits konfigurierten Providern oder Modellen zu wechseln. Wähle die genaue Model ID aus der <a href={"{{model-plaza-url}}"}>model plaza</a>.

## Häufige Fehler

| Fehler | Prüfen |
| --- | --- |
| `401` oder `Unauthorized` | Prüfe den BetterToken API Key und entferne führende oder nachgestellte Leerzeichen. |
| `404` | Prüfe die Base URL und hänge keinen spezifischen Endpunktpfad an. |
| `model not found` | Kopiere eine aktuelle Model ID aus der model plaza. |
| Änderungen werden nicht angewendet | Speichere die Konfiguration und starte das Tool vollständig neu. |


### Fehlerbehebung

- Wenn Requests fehlschlagen oder keine Antwort zurückgeben, bestätige, dass **Base URL** genau `{{openai-base-url}}` ist. Hänge in diesem Feld nicht `/chat/completions` an.
- Wenn Hermes meldet, dass das Modell nicht existiert, bestätige, dass **Model** eine Model ID eines unterstützten Providers aus der <a href={"{{model-plaza-url}}"}>model plaza</a> ist.
- Wenn die Authentifizierung fehlschlägt, prüfe auf kopierte Leerzeichen und bestätige, dass die ausgewählte Model ID vom **unterstützten Provider** stammt.
- Wenn Hermes nach einer Änderung weiterhin das alte Modell verwendet, beende die aktuelle Hermes-Agent-Sitzung und führe `hermes` erneut aus.
- BetterToken benötigt normalerweise keinen zusätzlichen `User-Agent` oder eigene Header. Füge Header nur hinzu, wenn dein eigenes Gateway, Tenant-Routing, deine Observability- oder Caching-Schicht sie ausdrücklich verlangt.

<Note>
  Committe keine echten API Keys in dein Repository. Bevorzuge für teamübergreifende Einrichtung Umgebungsvariablen oder lokale Konfiguration.
</Note>

### Verwandte FAQ

- [MCP, API Key und Base URL: Unterschiede](/de/faq/concepts/mcp-vs-api-key-base-url)
- [Was sind model_provider, base_url und wire_api?](/de/faq/codex/model-provider-base-url-wire-api)
- [OpenAI-kompatible API in Cline konfigurieren](/de/faq/cline/openai-compatible-api)
- [BetterToken-Einrichtungsanleitung für OpenClaw](/de/ai-tools/openclaw)

## Advanced setup

{/* tool-provider-support:start */}
## Unterstützte Provider

| Provider | Status |
| --- | --- |
| Claude | Nicht unterstützt |
| GPT | Manuelle Konfiguration |
| Kimi | Manuelle Konfiguration |
| GLM | Manuelle Konfiguration |

<Note>Die Statusangaben gelten für die auf dieser Seite beschriebene BetterToken-Konfiguration.</Note>

<Accordion title="Bedeutung der Einrichtungsmethoden">
  - **Manuelle Konfiguration**: Gib API Key, Base URL und Model ein.
  - **Nicht unterstützt**: Derzeit gibt es keine verifizierte Methode für die direkte Verbindung.
</Accordion>
{/* tool-provider-support:end */}

## Technische Details

<Accordion title="Protokoll, Endpunkt und interne Provider-Felder">
  Diese Einrichtung verwendet `{{openai-base-url}}`. Das Tool hängt den OpenAI-kompatiblen Endpunktpfad an. Füge `/chat/completions` oder `/responses` nicht hinzu, sofern ein bestimmtes Feld dies nicht ausdrücklich verlangt.
</Accordion>
