---
title: "DeepSeek V4.1 Flash が正式リリース"
description: "BetterToken で Model ID deepseek-flash を指定し、OpenAI Chat Completions または Anthropic Messages から DeepSeek V4.1 Flash を利用できます。"
tag: "新着"
---

更新日：2026 年 9 月 11 日

**DeepSeek V4.1 Flash** が BetterToken で正式に利用できるようになりました。正確な Model ID は `deepseek-flash` です。リクエストを送る前に、<a href={"{{model-plaza-url}}"}>モデル広場</a>で現在のアクセス状況と料金を確認してください。

<Check>
  `model` に `deepseek-flash` を指定します。以前の期間限定テスト用 Model ID は現在のエントリではありません。
</Check>

## 現在利用できる設定

| 設定 | 値 |
| --- | --- |
| プロバイダー | **DeepSeek** |
| Model ID | `deepseek-flash` |
| OpenAI Base URL | `{{openai-base-url}}` |
| OpenAI Chat Completions | `POST /v1/chat/completions` |
| Anthropic Base URL | `{{anthropic-base-url}}` |
| Anthropic Messages | `POST /v1/messages` |
| BetterToken の状態 | 利用可能 |

クライアントに合わせて OpenAI Chat Completions または Anthropic Messages を選択してください。両方のプロトコルで同じ BetterToken API Key と Model ID を使用します。

## 使用のヒント

実際のワークフローで重要なタスクを使って DeepSeek V4.1 Flash と現在利用可能な別のモデルを比較してください。

- **コード変更**：両方のモデルに同じリポジトリタスクを与え、正確性、編集品質、追加の指示が必要な回数を比較する
- **Agent ワークフロー**：各モデルが複数手順の指示に従い、クライアントのツールを利用する信頼性を測る
- **推論品質**：同じプロンプトと評価基準で、難しいデバッグ、計画、分析タスクを比較する
- **レイテンシとコスト**：最初の token までの時間、完了までの時間、token 使用量、BetterToken の現在料金を記録する

一度の結果だけに頼らず、代表的なプロンプトを複数実行してください。公平に比較するため、クライアント、システムプロンプト、ツール定義、サンプリング設定を同じにします。

## クイックスタート

1. <a href={"{{register-url}}"}>BetterToken API Key を作成</a>するか、既存の Key の **Setup** を開きます。
2. `deepseek-flash` が利用可能かモデル広場で確認します。
3. **DeepSeek プロバイダー**を選択します。
4. 上の表からプロトコルに対応する Base URL を選び、完全な Model ID を入力します。
5. 長い評価を始める前に、小さなリクエストを送信します。

```bash
curl "{{chat-completions-url}}" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  --data '{
    "model": "deepseek-flash",
    "messages": [
      {
        "role": "user",
        "content": "Explain the likely cause of this failing test and propose the smallest fix."
      }
    ]
  }'
```

成功したレスポンスでは、モデルの出力が `choices[0].message.content` に返されます。SDK の例と完全なリクエスト形式は、[Chat Completions API リファレンス](/ja/api-reference/chat-completions)を参照してください。

## 期間限定テスト用 Model ID から移行する

設定で以前の期間限定テスト用 Model ID をまだ使用している場合：

1. `deepseek-v4.1-flash-expires-on-0910` を含むすべての設定を見つけます。
2. `deepseek-flash` に置き換えます。
3. 変更後にスモークテストを実行します。
4. モデル広場で現在のモデルエントリと料金を確認します。

## よくある質問

### この Model ID を本番環境で使用できますか？

はい。`deepseek-flash` を使用し、BetterToken API Key でモデルを利用できることをモデル広場で確認してください。

### Claude Code や Anthropic Messages API で使用できますか？

はい。Claude Code または Anthropic Messages では Base URL `{{anthropic-base-url}}` と Model ID `deepseek-flash` を使用してください。Messages を直接呼び出す場合は `POST /v1/messages` を使用し、`x-api-key` ヘッダーに BetterToken API Key を指定します。

### 現在の料金はどこで確認できますか？

リアルタイムの<a href={"{{model-plaza-url}}"}>モデル広場</a>を確認してください。提供状況と料金は変更される場合があります。

## 関連ドキュメント

- [BetterToken Chat Completions API リファレンス](/ja/api-reference/chat-completions)
- [Anthropic Messages API リファレンス](/ja/api-reference/messages)
